# Roc 任务工作台重构计划 V1.0

| 字段    | 内容                                       |
| ----- | ---------------------------------------- |
| 文档状态  | 第一版设计 + 实施计划                              |
| 日期    | 2026-05-21                               |
| 事实源   | `想法.md`、`具体技术实现.md`、当前 `src/` 代码现状        |
| 适用范围  | 任务工作台 (`views/tasks`) 及其依赖的服务、调度器、AI 工具链  |
| 不适用范围 | 记忆系统、MCP、Skills、设置页面、Doctor 主链路（仅扩展少量检查项） |

---

## 0. 上下文与问题陈述

### 0.1 当前现状

- 数据层已具备 `TaskThread / TaskRun / TaskEvent / BackgroundTask` 四类模型与对应 SQLite 表（`task-service.ts`、`database-service.ts:40-93`）。
- IPC 已暴露 9 个 `tasks.*` 通道（`shared/ipc.ts:105-113`）。
- 渲染端 `TasksView.tsx`（182 行）目前仅是一个统计仪表面板：4 个指标 + 1 条当前后台任务行 + 暂停/继续/取消按钮。

### 0.2 关键缺口

| 缺口 | 影响 |
| --- | --- |
| 无调度器实现 | grep 全项目找不到 `TaskScheduler / cron / setInterval` 调度，`nextRunAt` 仅是数据库文本，到点不会真正触发 |
| 无自然语言 → 后台任务的桥 | AI 无法把"帮我建一个定时任务"落地到 `createBackgroundTaskPreview` |
| 无创建 / 编辑 UI | 工作台只能显示一个 `backgroundTask`，无新建表单与详情页 |
| 无"长会话"概念 | 普通 chat thread 与 backgroundTask 在 UI 上完全两条线 |

### 0.3 目标

1. 用户在聊天里自然语言创建定时 / 后台任务，AI 通过受控工具落库，用户审批后生效；后续可在工作台查看 / 修改 / 取消。
2. 单次长对话（如 Deep Agent 派生子代理或 ≥ 90 秒持续运行）自动晋升进入工作台。
3. 实现真正的调度器：到点触发、错过补偿、系统休眠恢复、`failurePolicy: pause_and_report` 语义化。
4. 工作台 UI 从"统计面板"重构为"活跃任务中心"（列表 + 详情抽屉 + 审批卡片）。

### 0.4 已对齐的关键决策

| 维度 | 决策 |
| --- | --- |
| 任务范畴 | 后台 / 定时任务 + 当前正在运行的对话任务（统一"活跃任务"视图） |
| AI 创建机制 | Tool + Interrupt（复用现有 PermissionService / interrupt / 审计链路） |
| 调度器形态 | 主进程内轻量调度器（自实现 setTimeout-based，不引入第三方库） |
| 修改交互 | 跳回聊天 + 上下文注入 + AI 调 `update_background_task` 工具（同一审批机制） |

### 0.5 参考开源项目

| 项目 | 借鉴点 |
| --- | --- |
| Open-Interpreter / Continue / Cline | 单次长任务在 UI 里的轨迹卡片、子任务收口 |
| Temporal（思想）/ BullMQ（轻量实现） | 调度模型、重试策略、失败处理 |
| AutoGen Studio / LangGraph Studio | 任务列表 + 详情 + 重运行 |
| n8n / Activepieces | 自然语言 → 工作流预览的确认流程 |
| VSCode Tasks | 任务定义 schema 与运行视图分离 |
| docs/references/ 中的 Open SWE / Hermes 报告 | 长任务事件流呈现 |

---

## 1. 概念边界与数据模型

### 1.1 概念定义（统一术语）

```
Task（任务）= TaskThread（数据库 id） + 可选 BackgroundTaskBinding
  - 普通对话：只有 TaskThread，没有 binding
  - 后台/定时任务：TaskThread + BackgroundTaskBinding（含 trigger / schedule / policy）
  - 长会话：普通 TaskThread，因 status=running 长时间不收口，自动进入工作台
```

唯一主键是 `taskThreadId`。`BackgroundTask` 退化为对 thread 的一层"调度 / 策略附加"。

### 1.2 数据库变更（schema_version 2）

#### 1.2.1 `background_tasks` 表新增列

```sql
ALTER TABLE background_tasks ADD COLUMN cron_expression  TEXT NULL;
ALTER TABLE background_tasks ADD COLUMN last_run_at      TEXT NULL;
ALTER TABLE background_tasks ADD COLUMN last_run_status  TEXT NULL;
ALTER TABLE background_tasks ADD COLUMN run_count        INTEGER NOT NULL DEFAULT 0;
```

#### 1.2.2 `task_threads` 表新增列

```sql
ALTER TABLE task_threads ADD COLUMN kind TEXT NOT NULL DEFAULT 'chat';
-- 值域: 'chat' | 'background' | 'long_running'
```

#### 1.2.3 新表 `scheduled_task_runs`

```sql
CREATE TABLE IF NOT EXISTS scheduled_task_runs (
  id                  TEXT PRIMARY KEY,
  background_task_id  TEXT NOT NULL,
  task_run_id         TEXT NULL,
  scheduled_at        TEXT NOT NULL,
  triggered_at        TEXT NULL,
  status              TEXT NOT NULL,
  skip_reason         TEXT NULL,
  FOREIGN KEY(background_task_id) REFERENCES background_tasks(id)
);

CREATE INDEX IF NOT EXISTS idx_scheduled_task_runs_task_status
  ON scheduled_task_runs(background_task_id, status, scheduled_at DESC);

CREATE INDEX IF NOT EXISTS idx_task_threads_kind_status
  ON task_threads(kind, status, updated_at DESC);
```

### 1.3 类型扩展（`src/shared/types/task.ts`）

```ts
export type TaskKind = 'chat' | 'background' | 'long_running';

export type BackgroundTaskTrigger =
  | { type: 'manual'; description: string }
  | { type: 'once'; description: string; nextRunAt: string }                                  // ISO
  | { type: 'cron'; description: string; cronExpression: string; nextRunAt: string };

export type BackgroundTask = {
  // 保留所有现有字段
  cronExpression: string | null;
  lastRunAt: string | null;
  lastRunStatus: 'success' | 'failed' | 'cancelled' | null;
  runCount: number;
};

export type ActiveTaskItem = {
  kind: 'background' | 'long_running';
  threadId: string;
  taskId: string | null;              // 仅 background 有
  title: string;
  goal: string;
  status: TaskStatus;
  trigger: BackgroundTaskTrigger | null;
  nextRunAt: string | null;
  lastRunAt: string | null;
  riskLevel: ShellCommandRisk;
  workspacePath: string | null;
  createdAt: string;
  updatedAt: string;
};

export type ScheduledTaskRun = {
  id: string;
  backgroundTaskId: string;
  taskRunId: string | null;
  scheduledAt: string;
  triggeredAt: string | null;
  status: 'pending' | 'fired' | 'skipped' | 'failed';
  skipReason: string | null;
};

export type TaskDetail = {
  threadId: string;
  taskId: string | null;
  thread: TaskThread;
  backgroundTask: BackgroundTask | null;
  lastRunId: string | null;
  runHistory: TaskRun[];
  recentEvents: TaskEvent[];
  schedulerRegistered: boolean;
};

export type UpdateBackgroundTaskRequest = {
  taskId: string;
  patch: Partial<BackgroundTaskPreviewRequest>;
  reason: string;
};

export type TaskUpdateEvent =
  | { kind: 'task_created'; taskId: string }
  | { kind: 'task_status_changed'; taskId: string; status: TaskStatus }
  | { kind: 'task_run_fired'; taskId: string; runId: string }
  | { kind: 'thread_promoted'; threadId: string; reason: string }
  | { kind: 'scheduler_health_changed'; healthy: boolean };

export type SchedulerStatus = {
  running: boolean;
  registeredTaskCount: number;
  nextFireAt: string | null;
  recentSkippedCount: number;
  lastError: string | null;
};

export type TaskEvent['type']        // 扩展联合
  | 'long_running_promoted';
```

---

## 2. 调度器架构

### 2.1 服务定位

新增 `TaskSchedulerService`（`src/main/services/task-scheduler-service.ts`），与 `TaskService` 并列。只负责"到点把 backgroundTask 转成一次 TaskRun"，不参与状态机裁决、权限、审计写入。

### 2.2 启动 / 关闭流程

接入位置：`src/main/services/app-service.ts` 启动序列中，**在 `DeepAgentRuntimeService` 之后**、**在 `WindowService` 之前**。

```
启动:
  1. TaskSchedulerService.start()
  2. SELECT * FROM background_tasks
       WHERE status IN ('running', 'pending_confirmation', 'paused') AND scheduled = 1
  3. 对每个有效条目计算下一次触发时间并注册定时器
  4. catch-up: nextRunAt < now() AND status='running' 的任务，按 failurePolicy 决定补跑或 skip

关闭:
  1. 清空所有定时器
  2. flush scheduled_task_runs 写入
  3. 标记仍在 fired 但未完成的 task_runs 状态为 paused 并记 background_task_paused 事件
```

### 2.3 触发机制（不引入第三方依赖）

第一版采用**滚动 setTimeout**策略，不引入 `croner` / `node-cron`：

```ts
class TaskSchedulerService {
  private timers = new Map<string, NodeJS.Timeout>();

  registerTask(task: BackgroundTask): void {
    const next = computeNextRunAt(task);
    if (next === null) return;
    const delay = Math.max(0, new Date(next).getTime() - Date.now());
    // setTimeout 最大 2^31-1 ≈ 24.8 天，超过则挂中转 timer 到点重算
    const safeDelay = Math.min(delay, 24 * 24 * 60 * 60 * 1000);
    const timer = setTimeout(() => this.fire(task.id), safeDelay);
    this.timers.set(task.id, timer);
  }
}
```

不引入第三方 cron 库的原因：

- Roc 第一版只需 5 段标准 cron + 一次性 ISO 时间，自实现可控
- 第三方 cron 库在主进程长驻 / 系统休眠 / 时区切换时行为不一
- 与 `想法.md`"默认安全 + 显式放权"一致：每一次触发用户能预见

### 2.4 `computeNextRunAt` 规则

| trigger.type | 计算方式 |
| --- | --- |
| `manual` | `null`（永不自动触发） |
| `once` | 直接返回 `nextRunAt`；触发后任务自动转 `completed` |
| `cron` | 主进程本地时区解析 cron 表达式，返回下一个匹配点的 ISO |

cron 解析自实现 `cron-parser.ts`（~80 行），覆盖五段标准 cron，**不支持** `@yearly` / 其他高级语法。解析失败显式抛 `background_task_cron_invalid`，不静默 fallback。

### 2.5 一次触发的完整链路

```
TaskSchedulerService.fire(taskId)
  -> 写 scheduled_task_runs(pending)
  -> taskService.markBackgroundTaskFiring(taskId)
  -> 前置校验: status='running' / 工作区可达 / 模型可用 / 后台未被全局暂停
       任何一项失败 → scheduled_task_runs(skipped, skip_reason=...) + failurePolicy 处理
  -> taskService.createTaskRun({...prefilledFromBackground})
  -> deepAgentRuntimeService.startRun({ mode: 'task', threadId, input: backgroundTask.goal, ... })
  -> 写 scheduled_task_runs(fired, task_run_id=...)
  -> backgroundTask.lastRunAt / runCount 更新
  -> 注册下一次 timer（cron 情形）
```

`failurePolicy: pause_and_report` 的语义被显式实现：单次触发失败 → 任务转 `paused` + 写 `background_task_paused` 事件 + tray 通知；下次到点不会触发。

### 2.6 与生命周期"全局暂停后台"的协调

`lifecycleService.pauseBackgroundExecution()` 当前只更新 tray 文本。重构后真正接管调度器：调用时 `TaskSchedulerService.suspendAll()`（清定时器，不动数据库 status，因为这是用户级"暂停后台"而非任务级暂停）；恢复时 `resumeAll()` 重新注册。

### 2.7 系统休眠 / 锁屏

Electron `powerMonitor.on('resume', ...)` 接入：从休眠恢复后扫描所有定时器，对 `nextRunAt < now()` 的执行 catch-up（按 failurePolicy）。Doctor 检查项覆盖。

### 2.8 不做项

- ❌ 不支持秒级 cron（5 段，分钟为最细粒度）
- ❌ 不支持时区指定（统一主进程本地时区，UI 显示带时区缩写）
- ❌ 不做多机分布式锁（Roc 是单机桌面）
- ❌ 不做"错过即重发 N 次"的复杂补偿（只做单次 catch-up）

---

## 3. AI 工具链（propose / update / cancel）

### 3.1 新增 3 个 Roc tool

注册位置：`src/main/services/deep-agent/background-task-tools.ts`，由 `deep-agent-runtime-service.ts` 在装配 tools 时加入。

| Tool name | 风险 | scope | interrupt | 用途 |
| --- | --- | --- | --- | --- |
| `propose_background_task` | `high` | `app` | ✅ 必须 | AI 提议创建新后台 / 定时任务 |
| `update_background_task` | `high` | `app` | ✅ 必须 | AI 修改现有任务 |
| `cancel_background_task` | `high` | `app` | ✅ 必须 | AI 请求取消任务 |

所有副作用（写 `background_tasks` 表）发生在 interrupt 批准之后。

### 3.2 工具签名（Zod schema）

```ts
const ProposeInput = z.object({
  goal: z.string().min(1).max(500),
  trigger: z.discriminatedUnion('type', [
    z.object({ type: z.literal('manual'), description: z.string() }),
    z.object({ type: z.literal('once'),   description: z.string(), nextRunAt: z.string().datetime() }),
    z.object({ type: z.literal('cron'),   description: z.string(), cronExpression: z.string() }),
  ]),
  workspacePath: z.string().min(1),
  allowedActions:   z.array(z.string()).default([]),
  forbiddenActions: z.array(z.string()).default([]),
  notificationPolicy: z.literal('failures_and_confirmations').default('failures_and_confirmations'),
});

const UpdateInput = z.object({
  taskId: z.string().min(1),
  patch: ProposeInput.partial(),
  reason: z.string().min(1),
});

const CancelInput = z.object({
  taskId: z.string().min(1),
  reason: z.string().min(1),
});
```

模型生成的 `cronExpression` 在 interrupt **预校验阶段**由 `parseCron()` 验证，失败直接拒绝（不进入审批），错误码 `background_task_cron_invalid`，强制模型重试。

`failurePolicy` 第一版固定为 `pause_and_report`，不暴露给模型，避免幻觉。

### 3.3 工具执行体（interrupt 之前）

```ts
async function handler(input, ctx) {
  // 1. 预校验：cron 表达式 / 工作区路径合法 / nextRunAt 不在过去 5 分钟外
  const preview = taskService.createBackgroundTaskPreview({
    ...input,
    failurePolicy: 'pause_and_report',
  });

  // 2. 抛出结构化 interrupt
  return interrupt({
    kind: 'propose_background_task',
    preview,
    risk: preview.riskLevel,
    requiredFields: ['decision'],
  });
}
```

| 用户决策 | 实际副作用 |
| --- | --- |
| `approve` | `createBackgroundTask(preview)` → 注册到 scheduler → 返回 `{ok: true, taskId, scheduledNextRunAt}` 给模型 |
| `edit` | 用户修改后的 preview 再走 createBackgroundTask |
| `reject` | 返回 `{ok: false, reason}` 给模型，任务不入库 |

### 3.4 复用现有 interrupt 链路

不新增审批通道。`agent-service.ts: createInterruptPolicy` 加入：

```ts
'propose_background_task': {
  allowedDecisions: ['approve', 'edit', 'reject'],
  description: '创建后台 / 定时任务',
  requiredFields: ['decision'],
  riskLevel: 'high',
},
'update_background_task': { /* 同上 */ },
'cancel_background_task': {
  allowedDecisions: ['approve', 'reject'],   // 不允许 edit
  description: '取消已有后台任务',
  requiredFields: ['decision'],
  riskLevel: 'high',
},
```

恢复链路通过现有 `DeepAgentRuntimeService.resumeRun()` 完成，**无需新 IPC 通道**。

### 3.5 "让 AI 修改任务"的上下文注入

任务详情页"让 AI 修改"按钮的实现：

1. 渲染端调用新 IPC：`tasks.openInChat(taskId)`
2. 主进程：
   - 取出该 `backgroundTask` 当前完整 JSON
   - 复用 `backgroundTask.threadId` 作为对话 thread
   - 写一条 system 类型的 `task_events`：
     ```
     [系统] 用户正在请求修改后台任务 task_xxx。当前定义：
     <BackgroundTask JSON pretty-printed>
     你可以调用 update_background_task 工具完成修改。
     ```
3. 渲染端路由切换到 chat view 并选中该 thread

这条 system 事件是**临时上下文注入**，不进入策展记忆，不污染 `hot_memory.md`。

### 3.6 与现有 chat 升级路径的协调

模型在普通 chat thread 中调用 `propose_background_task` → 用户 approve 后，当前 thread 自动 `kind: 'chat' → 'background'`，但不改变 thread 自身的执行状态。后续定时触发向同一 thread 追加新的 run。

### 3.7 不做项

- ❌ 不让模型自己决定 `failurePolicy`
- ❌ 不让模型设置长期授权（`allowedActions` 只对单个 task 生效）
- ❌ 不支持"列出所有后台任务"工具（避免上下文污染）

---

## 4. 工作台 UI 重构

### 4.1 目录与文件结构

```
src/renderer/views/tasks/
  TasksView.tsx                     # 列表页（重写，~80 行）
  TaskListSection.tsx               # 各分组列表
  TaskRow.tsx                       # 单行任务卡
  TaskDetailDrawer.tsx              # 右侧滑出详情面板
  TaskCreateDialog.tsx              # 手动新建/编辑表单（兜底入口）
  TaskApprovalCard.tsx              # chat 流里的 propose/update interrupt 审批卡
  task-view-model.ts                # 纯函数：LoadedState → ActiveTaskItem[]
  use-task-actions.ts               # hook：pause/resume/cancel/openInChat/runNow
```

### 4.2 分组规则

| 分组 | 包含 status | 默认展开 |
| --- | --- | --- |
| 待确认 | `pending_confirmation` + 有 pending approval 的 thread | ✅ |
| 运行中 | `running` + `waiting_user` + `waiting_next_turn` | ✅ |
| 计划中 | `scheduled = true` 且未到第一次运行 | ✅ |
| 已暂停 | `paused` | 折叠 |
| 已失败 | `failed` | 折叠 |
| 最近完成 | `completed` 且 7 天内 | 折叠 |

### 4.3 任务行

每行展示：

- **第一行**：goal（最多 1 行截断）+ 触发类型 tag + 主操作按钮
- **第二行（meta）**：状态描述 · 风险 · 工作区相对路径
- **悬浮**：完整 goal、createdAt、lastRunAt

主操作按钮按状态切换：

| 状态 | 主按钮 | 次按钮（"…"菜单） |
| --- | --- | --- |
| `pending_confirmation` | 处理审批 | 取消 |
| `running` | 查看 | 暂停 · 取消 |
| `paused` | 继续 | 立即运行 · 取消 · 删除 |
| `failed` | 查看诊断包 | 重试 · 删除 |
| `completed` | 查看 | 重新创建（复制定义） |

### 4.4 任务详情抽屉

右侧抽屉（不破坏现有四面板布局），分 4 个 tab：概览 / 运行历史 / 事件流 / 设置。底部操作：让 AI 修改 · 立即运行 · 暂停 · 打开聊天 · 复制 ID。

### 4.5 手动新建 / 编辑对话框

虽然主路径是"AI 创建"，仍提供手动入口作为兜底。表单字段与 `propose_background_task` schema 一一对应。提交不走 interrupt（既然用户已在表单里看见完整字段，再弹审批冗余）。例外：`riskLevel === 'high' / 'critical'` 或 `forbiddenActions.length === 0 && allowedActions.length === 0` 时仍需额外确认。

### 4.6 审批卡片

chat 流中出现 `propose_background_task` / `update_background_task` interrupt 时由 chat-transcript-panel 渲染：

```
┌─ AI 提议：创建定时任务 ──────────────┐
│ 目标:     每天检查项目测试状态        │
│ 触发:     每天 09:00 (cron: 0 9 * * *)│
│ 工作区:   F:\Code\Roc                 │
│ 允许动作: pnpm test                   │
│ 禁止动作: git push                    │
│ 风险:     medium                      │
│ [✓ 批准创建] [✎ 编辑后批准] [✗ 拒绝]    │
└───────────────────────────────────────┘
```

"编辑后批准"展开为内嵌表单（不弹新对话框），改完点确认走 `chat.resumeRun({decision: 'edit', value: ...})`。

### 4.7 左侧导航变化

`nav-items.ts: buildWorkspaceNavItems` 中 `tasks` 项 meta：

```ts
meta: `${activeCount} 活跃 · ${pendingApprovalCount} 待确认 · ${scheduledCount} 定时`
```

`activeCount = running + waiting_user + waiting_next_turn + paused + pending_confirmation`

### 4.8 空态

- 完全无任务 → EmptyState：「在聊天里告诉我『帮我创建一个定时任务』，或点击右上角"新建任务"」
- 某分组无任务 → 折叠时 `(0)`，展开后整段隐藏

### 4.9 不做项

- ❌ 拖拽排序、多选批量、搜索框、日历视图

---

## 5. 长会话晋升逻辑

### 5.1 晋升条件

`kind` 从 `chat` 提升为 `long_running`，满足下列**任一**：

| 条件 | 阈值（settings 可调） |
| --- | --- |
| status 持续为 `running` 超过 | `90 秒` |
| 单轮 run 累计 `tool_call` 事件超过 | `8 次` |
| thread 内累计 `subagent_started` 事件超过 | `1 次` |
| `interruptOn` 触发等待用户审批 | 立即 |
| thread 字段 `kind = 'background'` | 立即（已是后台任务） |

**降级**仅在 thread `status` 终态时移出工作台。已晋升不会因后续 run 变短而退回 `chat`，避免边界值抖动。

### 5.2 判定位置

`TaskService.evaluateLongRunningPromotion(threadId)`，调用点：

- `recordEvents()` 写完事件后
- `markRunRunning()` / `markRunWaitingUser()` 状态变化后
- `DeepAgentRuntimeService.startRun()` 末尾 `setTimeout(() => evaluate(threadId), 90_000)`（run 提前结束清除）

evaluate 是幂等的：`UPDATE ... WHERE kind = 'chat'` 单条原子操作。

### 5.3 事件流

新增 `TaskEvent.type`：`long_running_promoted`

```ts
{
  type: 'long_running_promoted',
  payload: {
    reason: 'running_over_90s' | 'tool_calls_exceeded' | 'subagent_spawned' | 'manual',
    threshold: number | null,
    observedValue: number | null,
  }
}
```

UI 在 chat-transcript 渲染一行细信息："本对话已进入任务工作台 · 触发原因：派生了子代理"，附"在工作台查看"链接。

### 5.4 手动晋升

chat composer 旁的"…"菜单提供"标记为长任务"，调用 `tasks.promoteThread(threadId, { reason: 'manual' })`。不需要 interrupt（用户已显式操作）。

不提供"取消晋升"：一旦进入工作台，要么完成要么取消整个 thread，避免审计盲点。

### 5.5 与历史对话侧边栏的边界

`src/renderer/history-sidebar.ts` 改造：

```ts
const promotedThreadIds = new Set(
  threads.filter((t) => t.kind === 'long_running' || t.kind === 'background').map((t) => t.id)
);
return threads.filter((t) =>
  !(promotedThreadIds.has(t.id) &&
    ['running','waiting_user','waiting_next_turn','paused','pending_confirmation'].includes(t.status))
);
```

避免 thread 在"历史对话"与"任务工作台"同时出现；已 completed 的 long_running thread 回到历史。

### 5.6 settings 阈值

```jsonc
{
  "tasks": {
    "longRunningThresholds": {
      "runningSeconds": 90,
      "toolCallCount": 8,
      "subagentCount": 1
    }
  }
}
```

第一版**不**提供阈值的 UI，只接受默认值（保留扩展点）。`ConfigService.getTaskSettings()` 提供读取，evaluate 每次调用前读取（不缓存）。

### 5.7 不做项

- ❌ token 累计阈值（provider 差异大）
- ❌ AI 自我判定是长任务（幻觉风险）
- ❌ 晋升通知（避免与"任务完成通知"重复打扰）

---

## 6. IPC 与 preload 扩展

### 6.1 新增 IPC 通道（`src/shared/ipc.ts: ipcChannels`）

```ts
tasksGetActiveTasks:        'roc:tasks:get-active-tasks',
tasksGetTaskDetail:         'roc:tasks:get-task-detail',
tasksListScheduledRuns:     'roc:tasks:list-scheduled-runs',
tasksRunBackgroundNow:      'roc:tasks:run-background-now',
tasksDeleteBackgroundTask:  'roc:tasks:delete-background-task',
tasksUpdateBackgroundTask:  'roc:tasks:update-background-task',
tasksOpenInChat:            'roc:tasks:open-in-chat',
tasksPromoteThread:         'roc:tasks:promote-thread',
tasksGetSchedulerStatus:    'roc:tasks:get-scheduler-status',
```

AI 创建走 `chat.resumeRun` 已有路径，**不**新加专用通道。

### 6.2 preload API（`RocPreloadApi.tasks`）

```ts
tasks: {
  // 现有 9 个保留
  getSnapshot, getThreadMessages, listBackgroundTasks, deleteThread,
  createBackgroundTaskPreview, createBackgroundTask,
  pauseBackgroundTask, resumeBackgroundTask, cancelBackgroundTask,
  onUpdated,

  // 新增 9 个
  getActiveTasks:       () => Promise<IpcResult<ActiveTaskItem[]>>;
  getTaskDetail:        (req: { taskId: string }) => Promise<IpcResult<TaskDetail>>;
  listScheduledRuns:    (req: { taskId: string; limit?: number }) => Promise<IpcResult<ScheduledTaskRun[]>>;
  runBackgroundNow:     (id: string) => Promise<IpcResult<{ taskId: string; runId: string }>>;
  deleteBackgroundTask: (id: string) => Promise<IpcResult<{ deleted: true }>>;
  updateBackgroundTask: (req: UpdateBackgroundTaskRequest) => Promise<IpcResult<BackgroundTask>>;
  openInChat:           (req: { taskId: string }) => Promise<IpcResult<{ threadId: string }>>;
  promoteThread:        (req: { threadId: string; reason: 'manual' }) => Promise<IpcResult<TaskThread>>;
  getSchedulerStatus:   () => Promise<IpcResult<SchedulerStatus>>;
}
```

### 6.3 错误码

```
background_task_cron_invalid          -- cron 表达式解析失败
background_task_schedule_in_past      -- once 触发的 nextRunAt 已过期 > 5 分钟
background_task_workspace_unreachable -- 工作区路径不可达
background_task_delete_not_terminal   -- 试图删除非终态任务
background_task_already_running       -- 试图 runNow 但当前正在运行
scheduler_not_started                 -- 调度器未启动
scheduler_resource_exhausted          -- 同时注册任务数 > 256
long_running_threshold_invalid        -- settings 阈值为负或溢出
```

### 6.4 事件总线变化

现有 `tasks.onUpdated()` 是空回调，重构为带 payload：

```ts
onUpdated: (cb: (event: TaskUpdateEvent) => void) => () => void;
```

渲染端按 `kind` 决定刷新粒度，避免每次全量 `getActiveTasks`。

### 6.5 删除的冗余字段

- `state.backgroundTask: BackgroundTask | null` → 删除
- `state.backgroundTasks: BackgroundTask[]` → 由 `state.activeTasks: ActiveTaskItem[]` 替代
- `state.taskSnapshot.recentEvents` 不再驱动工作台（保留供历史侧边栏使用）

---

## 7. 启动顺序、文件级改动与数据库迁移

### 7.1 启动顺序（`app-service.ts`）

```
现有: ConfigService → DatabaseService → ... → DeepAgentRuntimeService → WindowService → TrayService

新增: DeepAgentRuntimeService 之后、WindowService 之前
      TaskSchedulerService(taskService, deepAgentRuntimeService, lifecycleService).start()
      // 异步但不阻塞主流程；失败写 Doctor finding，不阻断 UI
```

退出序列追加：`TaskSchedulerService.stop()` 优先清定时器。

### 7.2 数据库迁移（schema_version 2）

按现有 `app_config_versions` 机制走，DDL 见 §1.2。迁移失败时启动阶段抛 `database_unavailable`，与现有错误分类一致。

### 7.3 文件级改动清单

#### 新增文件

```
src/main/services/task-scheduler-service.ts                 # ~280 行
src/main/services/task/cron-parser.ts                       # ~80 行
src/main/services/task/next-run-calculator.ts               # ~40 行
src/main/services/task/long-running-evaluator.ts            # ~60 行
src/main/services/task/active-task-projection.ts            # ~70 行
src/main/services/deep-agent/background-task-tools.ts       # ~200 行
src/renderer/views/tasks/TaskListSection.tsx                # ~80 行
src/renderer/views/tasks/TaskRow.tsx                        # ~100 行
src/renderer/views/tasks/TaskDetailDrawer.tsx               # ~180 行
src/renderer/views/tasks/TaskCreateDialog.tsx               # ~200 行
src/renderer/views/tasks/TaskApprovalCard.tsx               # ~120 行
src/renderer/views/tasks/task-view-model.ts                 # ~80 行
src/renderer/views/tasks/use-task-actions.ts                # ~100 行
tests/main/task-scheduler-service.test.ts
tests/main/task-cron-parser.test.ts
tests/main/task-long-running-evaluator.test.ts
tests/main/background-task-tools.test.ts
tests/renderer/task-view-model.test.ts
```

#### 修改文件

```
src/shared/types/task.ts                  # 新增类型
src/shared/ipc.ts                         # 新增 9 通道 + preload 签名
src/preload/index.ts                      # 透传 9 新通道
src/main/ipc/register-ipc.ts              # 注册 9 个 handler
src/main/services/database-service.ts     # schema_version 2 迁移
src/main/services/task-service.ts         # 新方法：getActiveTasks/getTaskDetail/update/delete/runNow/promoteThread/evaluateLongRunningPromotion
src/main/services/app-service.ts          # 启动序列接入 scheduler
src/main/services/agent-service.ts        # interrupt policy 加 3 个 tool
src/main/services/deep-agent-runtime-service.ts  # tools 装配处加 background-task-tools
src/main/services/lifecycle-service.ts    # pause/resume 真正调度 scheduler
src/main/services/window-shell.ts         # focusMainAndNavigate 复用或补充
src/renderer/loaded-state.ts              # 删除 backgroundTask，新增 activeTasks
src/renderer/app/data-loading.ts          # 启动加载 activeTasks
src/renderer/app/nav-items.ts             # buildWorkspaceNavItems meta 改造
src/renderer/history-sidebar.ts           # filter 增加 kind 判定
src/renderer/views/tasks/TasksView.tsx    # 重写为编排层（~80 行）
src/renderer/chat/chat-transcript-panel.tsx  # 渲染 TaskApprovalCard + long_running_promoted 提示行
```

#### 不动文件（明确边界）

```
src/main/services/deep-agent/ 内除 background-task-tools.ts 之外的所有文件
src/main/services/memory-service.ts 及全部 memory/ 子目录
src/main/services/git-service.ts
src/main/services/skill-service.ts / mcp-service.ts
src/renderer/chat/use-chat-run.ts                   # interrupt 恢复已有路径完全够用
```

### 7.4 配置文件变化

`%USERPROFILE%\.roc\config\settings.json` 追加可选段：

```jsonc
{
  "tasks": {
    "longRunningThresholds": {
      "runningSeconds": 90,
      "toolCallCount": 8,
      "subagentCount": 1
    },
    "scheduler": {
      "catchUpOnStartup": true,
      "maxRegisteredTasks": 256
    }
  }
}
```

`ConfigService.getTaskSettings(): TaskSettings`：对缺失字段填默认值（缺失即合法默认，不掩盖关键字段缺失）。

### 7.5 Doctor 检查项扩展（`diagnostics-service.ts`）

| Check ID | 说明 | finding |
| --- | --- | --- |
| `scheduler_running` | scheduler.status === 'running' | error · 未启动 |
| `scheduler_tasks_registered` | 注册数与 DB scheduled=1 数一致 | warning · 不一致 |
| `scheduler_missed_runs_recent` | 24h 内 status='skipped' 计数 | warning · > 0 |
| `cron_expressions_valid` | DB 中所有 cron_expression 逐条 parseCron | error · 任一失败 |

---

## 8. 测试、验证与不做项

### 8.1 Vitest 必测用例

#### 调度器（`task-scheduler-service.test.ts`）

- 注册 cron 任务后 `computeNextRunAt` 返回正确下一分钟点
- once 任务到期后转 `completed` 且不再注册新 timer
- pause 后定时器被清除
- resume 后下一次 nextRunAt 基于"当前时间"而非"原计划时间"重算
- 启动 catch-up：nextRunAt 已过期 30 分钟的任务按 pause_and_report 写 skipped 而非补跑
- 系统休眠模拟（`vi.useFakeTimers()`）→ resume 后正确补跑
- 同一任务并发触发保护（已 running 时再次 fire 应跳过，skip_reason=already_running）
- 超过 256 上限时新注册返回 `scheduler_resource_exhausted`

#### cron 解析（`task-cron-parser.test.ts`）

- `0 9 * * *` → 每天 9:00
- `*/15 * * * *` → 每 15 分钟
- `0 9 * * 1-5` → 工作日 9:00
- `0 22 * * 0` → 每周日 22:00
- 非法表达式（6 段、负数、超出范围）抛 `background_task_cron_invalid`
- DST 切换不重复触发（按系统时区，第一版不特殊补偿，测试明确此点）

#### 长任务晋升（`task-long-running-evaluator.test.ts`）

- 累计 8 次 tool_call 后下一次 evaluate 写入 `kind='long_running'`
- 派生子代理事件立即触发晋升
- 已是 long_running 再次 evaluate 不重复写入
- completed 后不会回退为 chat
- settings 阈值改为 5 时按新值判定

#### Background Task Tools（`background-task-tools.test.ts`）

- propose 输入合法 → 不直接落库，返回 interrupt payload
- 输入 cron 非法 → 不进入审批，直接抛 `background_task_cron_invalid`
- 用户 approve resume 后 background_tasks 表新增一行，调度器收到注册
- 用户 edit resume 时编辑后字段 reach 数据库
- 用户 reject resume 不落库
- update 修改 cron 后调度器移除旧 timer 注册新 timer
- cancel 与现有 cancelBackgroundTask 共享同一 transition 逻辑

#### 视图模型（`task-view-model.test.ts`）

- 7 个分组边界用例
- 同一 thread 既是 background 又被晋升 long_running，分组优先用 background
- 已完成且 > 7 天的任务不出现在"最近完成"

### 8.2 Playwright/Electron Smoke

`tests/smoke/tasks-workbench.smoke.ts`：

1. 启动打包后 `.exe`
2. 点击侧边栏"任务工作台"
3. 看到空态文案
4. 点击"+ 新建任务" → 填表 → 创建一次性任务（nextRunAt = 当前 + 30 秒）
5. 等待 35 秒，断言：scheduled_task_runs 出现 fired 行，状态转 completed
6. renderer 不出现 `require` / `process`

### 8.3 性能 profiling

`scripts/smoke-performance.mjs` 加入：

- 50 个 cron 任务时主进程 RSS 增量 ≤ 5 MB
- 调度器空闲 CPU < 0.5%
- `getActiveTasks()` 在 100 任务规模下 ≤ 50 ms

### 8.4 验收标准（与 `想法.md §13` 衔接）

- 用户在聊天里说"帮我建一个每天 9 点检查测试的定时任务"，AI 弹审批卡；批准后任务出现在工作台"运行中"
- 应用关闭 8 小时再启动，错过的定时任务按 pause_and_report 记 skipped 而非自动补跑
- 任务详情页点"让 AI 修改"跳回 chat，AI 上下文中能看到完整 JSON 并能调用 update tool
- 一次性任务到期 30 秒内被触发且不再重复
- 系统休眠 1 小时后唤醒，已错过的任务在 60 秒内完成 catch-up 决策
- chat 中 Deep Agent 派生子代理后该 thread 自动出现在工作台"运行中"分组
- 工作台"已暂停 / 已失败"分组与 `tray.backgroundTasks.failed/paused` 计数一致

### 8.5 第一版不做项

- ❌ 任务模板库（"常用任务"快捷创建）
- ❌ 任务间依赖（A 完成后触发 B）
- ❌ 远程触发（HTTP webhook / OS 事件）
- ❌ 任务运行结果通知到外部（邮件 / 微信）
- ❌ 时区选择 UI
- ❌ 秒级 cron
- ❌ 任务运行历史可视化图表
- ❌ 任务搜索 / 标签 / 拖拽排序
- ❌ 任务导入导出

---

## 9. 实施阶段建议

按依赖关系顺序，分 5 个 phase。每个 phase 验收通过后再进入下一个，每个 phase 落 1-2 个 commit。

### Phase 1：数据模型与迁移

**产出**

- `src/shared/types/task.ts` 扩展（新增类型 / 联合扩展）
- `database-service.ts` schema_version 2 迁移
- `task-service.ts` 新增方法骨架（仅签名 + 留空 throw）

**验收**

- `vitest tests/main/database-indexes.test.ts` 通过
- 升级旧 .roc/roc.sqlite 后能正常加载新 schema

### Phase 2：调度器核心

**产出**

- `task-scheduler-service.ts`
- `task/cron-parser.ts` / `task/next-run-calculator.ts`
- `app-service.ts` 启动序列接入
- `lifecycle-service.ts` pause/resume 真正接管

**验收**

- 8 个调度器单测全部通过
- 手工：创建一个一次性 30 秒后触发的任务，到点 scheduled_task_runs 出现 fired 行

### Phase 3：AI 工具链

**产出**

- `deep-agent/background-task-tools.ts`
- `agent-service.ts` interrupt policy 扩展
- `deep-agent-runtime-service.ts` tools 装配处接入

**验收**

- 7 个 background-task-tools 单测全部通过
- 手工：聊天里说"帮我建一个 1 分钟后触发一次的任务"，弹审批卡，approve 后入库且 60 秒后触发

### Phase 4：工作台 UI

**产出**

- TasksView 重写（编排层）+ 7 个子组件
- task-view-model.ts + use-task-actions.ts
- nav-items.ts / history-sidebar.ts 调整
- chat-transcript-panel.tsx 渲染审批卡 + 晋升提示行

**验收**

- 5 个视图模型单测通过
- 手工：完整流程 — 聊天创建任务 → 工作台查看 → 让 AI 修改 → 暂停 / 继续 / 取消

### Phase 5：长会话晋升与 Doctor

**产出**

- `task/long-running-evaluator.ts`
- `task-service.ts: evaluateLongRunningPromotion`
- 调用点接入（recordEvents / markRunRunning / startRun 计时）
- `diagnostics-service.ts` 4 个新检查项
- smoke 用例 `tasks-workbench.smoke.ts`

**验收**

- 5 个晋升单测通过
- smoke 通过：打包 `.exe` 启动 → 创建一次性任务 → 触发 → 完成
- Doctor 页面看到 4 个新检查项均 pass

---

## 10. 风险与缓解

| 风险 | 缓解 |
| --- | --- |
| setTimeout 在主进程 sleep / suspend 行为不一致 | `powerMonitor.on('resume')` 触发 catch-up + Doctor 报告 |
| AI 生成的 cron 表达式不可解析 | interrupt 预校验阶段拒绝，强制模型重试，不进入审批 |
| 用户在审批卡中 edit 出非法字段 | 编辑后再次走 `createBackgroundTaskPreview` 校验，失败仍停在编辑态 |
| 同一 thread 同时被 long_running 晋升和 background 创建 | 分组优先用 background（thread.kind 用 background 覆盖 long_running） |
| 256 任务上限不够用 | 第一版假设单用户桌面够用，超出时 Doctor 报告 + 设置可调，但仍有硬上限保护 |
| 数据库迁移失败导致 `.exe` 无法启动 | 启动阶段抛 `database_unavailable`，与现有错误分类一致；保留旧数据库供回退 |

---

## 11. 外部文档依据

- Deep Agents JavaScript human-in-the-loop：`https://docs.langchain.com/oss/javascript/deepagents/human-in-the-loop`
- LangGraph interrupt / Command(resume=...)：`https://langchain-ai.github.io/langgraph/concepts/human_in_the_loop/`
- Electron powerMonitor：`https://www.electronjs.org/docs/latest/api/power-monitor`
- Croner（参考但未引入）：`https://github.com/Hexagon/croner`
- cron 表达式标准：`https://en.wikipedia.org/wiki/Cron`

---

## 12. 与 Roc 现有文档的衔接

| 文档 | 衔接点 |
| --- | --- |
| `想法.md §3.2 任务会话` | 本方案保持任务线程 / 执行轮次 / 追加指令的状态机 |
| `想法.md §7 后台任务、定时任务与自动化` | 本方案是其工程落地 |
| `想法.md §9 能力说明卡与授权` | propose/update/cancel 工具进入 interrupt 审批，与高风险动作一致 |
| `具体技术实现.md §7.4 工具注册` | 3 个新工具遵循 `RocToolDefinition` 接口规范 |
| `具体技术实现.md §19.3 interruptOn` | 复用 `RocInterruptPolicy` 配置 |
| `具体技术实现.md §20 后台任务` | 本方案补齐其 §20.2 `TaskSchedulerService` 实现 |
| `具体技术实现.md §22 Doctor 与诊断包` | 扩展 4 个新检查项 |
