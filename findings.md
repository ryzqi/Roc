# Roc Agent Harness 发现

## Requirements

- 严格按 `plan.md` Stage 0–7 顺序执行。
- 每部分完成后独立 review；有问题修复；无问题提交。
- 最终安全清理仅能证明无用的遗留代码。

## Current Findings

- 2026-07-18 Stage 4 Gate：用户确认 interactive shell 无需审批，目标是尽可能减少人工参与。执行仍真实标为 host code execution；background shell 保留任务创建时 durable pre-authorization，避免无用户发起的 schedule 获得 shell。
- 2026-07-18 Stage 4 研究：官方网页检索工具两次返回 `Invalid URL`，未继续重试。实现 API 以安装包声明与本机官方-skill 镜像为准；变更必须由 repository regression 覆盖。
- 2026-07-18 Stage 4 Part 1：`buildDeepAgent()` 原先只在 hook runtime 存在时显式创建 general-purpose subagent；无 hook 时 custom subagent 直接透传，缺 Roc shell/path/protocol/effect/error-budget 安全中间件，也不显式继承冻结 `interruptOn`。DeepAgents 1.10.7 声明确认 declarative `SubAgent` 支持 `middleware` 和 `interruptOn`，且自定义 middleware 附加在 native defaults 后。
- 2026-07-18 Stage 4 Part 1：新增 scope regression 先稳定失败于缺 general-purpose；修复后 main、general-purpose、research 都装配同一现有 safety 列表，main 仍保持 context compaction 在 rescue parsing 前的既有顺序。聚焦测试 1/1、既有 wiring 14/14、`pnpm typecheck` 通过。
- 2026-07-18 Stage 4 Part 1 independent review：Standards 无 findings。Spec P1：`DeepAgentBuildInput` 仅接收可变 tools/interruptOn，`RocToolProtocolMiddleware` 未按 frozen manifest 校验 canonical tool identity 或 main/subagent execution scope；必须让 executor 传入 immutable manifest，并在 shared safety middleware 拒绝未登记/错误 scope tool，再复审。
- 2026-07-18 Stage 4 Part 1 P1 repair：executor 现将已解析且 hash 校验过的 snapshot manifest 传入 builder；protocol middleware 在 handler 前校验 runtime tool identity、manifest authorization 与 execution scope，并只允许 DeepAgents built-ins、Roc internal runtime tools或 manifest 登记的工具。新增 red-green regression 覆盖未登记 shell、subagent scope denied、tool name drift。11 文件/71 测试、typecheck、strict unused、diff check 通过，等待全新独立复审。
- 2026-07-18 Stage 4 Part 1 re-review P1：两位 reviewer 一致指出 `ROC_RUNTIME_TOOL_NAMES` 是 manifest bypass；其中包含 model-visible write/edit/background-task tools，未受 execution scope 限制。必须将所有 Roc runtime tools 编入 frozen manifest 并删除名称 bypass，至少覆盖 subagent 拒绝 write/background mutation 的运行时回归。
- 2026-07-18 Stage 4 Part 1 P1 repairs：manifest 现在记录所有模型可见 Roc runtime tool 的 identity/effect/scope；background task 与 user/session tools 仅 `main`，filesystem write/edit 保留现有 main/subagent surface。protocol 已删除 runtime-name bypass，未知工具一律拒绝。compiled/async subagent 因无法附加同等 safety contract 而明确拒绝。11 文件/73 测试、typecheck、strict unused、diff check 通过，等待新独立复审。
- 2026-07-18 Stage 4 Part 1 final review P1：general-purpose 构造仍使用全量 main tools，使 `ask_user` 等 main-only tool 对 subagent 可见、随后才被 protocol 拒绝。必须在 builder 前按 manifest scope 过滤。P2：runtime manifest 无条件列入 background tools，但 executor 仅在 workflow hint 存在时注册；manifest 必须按 workflow 精确编译。复审还要求所有 builder fixture 使用真实 manifest，不以 unknown cast 遮蔽必填合同。
- 2026-07-19 Stage 4 Part 1 恢复：session catchup 无未同步上下文；CodeGraph 当前源确认 `createRunModeSubagents()` 将 main 全量 tools 交给 general-purpose，而 `createRuntimeManifestTools()` 无条件列 background tools；executor 仅在 `backgroundTaskToolMode !== null` 时注册对应 tools。修复边界保持为 subagent tool surface 预过滤、workflow-dependent frozen inventory、真实 manifest fixtures。
- 2026-07-19 Stage 4 Part 1 red state：`run-capability-manifest.test.ts` 已有普通 chat 不含 `schedule_background_task`、background workflow 包含该 tool 的期望；当前 compiler 无条件 inventory，预期稳定失败。`deep-agent-build-wiring.test.ts` 仍断言 general-purpose 获得 main 全量 tools，需换为同时含 main-only/subagent-allowed tool 的真实 manifest fixture。
- 2026-07-19 Stage 4 Part 1 fixture audit：未跟踪的 `execution-safety-scopes.test.ts` 已有 main-only tool exposure 测试，但一处手写无效 `manifestHash`，另一处完全漏 manifest，均由 `as unknown as DeepAgentBuildInput` 遮蔽。新合同测试必须调用真实 `compileRunCapabilityManifest()` 生成 manifest。
- 2026-07-19 Stage 4 Part 1 fixture scope：`deep-agent-build-wiring.test.ts` 有 14 个 direct build inputs，scope 文件有 3 个；新增必填 manifest 大多被整体双重 cast 遮蔽。只为这些 builder fixtures 注入同一真实 compiler 产物，不重写其余 stub 类型或无关测试结构。
- 2026-07-19 Stage 4 Part 1 type contract：本机 DeepAgents 1.10.7 声明 `SubAgent.tools?: StructuredTool[]`，因此可直接按 `tool.name` 对 general-purpose 与 declarative custom subagent 做 manifest scope 过滤；不需要字符串工具或兼容分支。
- 2026-07-19 Stage 4 Part 1 workflow matrix：`WorkflowHint` 仅 `null | propose_background_task | background_task_change`。对应 executor surface：null 无 background tools；propose 为 time/propose/schedule/read；change 为 read/update/cancel。Plan mode始终不注册 background tools。
- 2026-07-19 Stage 4 Part 1 green：compiler 现以必填 `workflowHint` 生成精确 background inventory；builder 在 DeepAgents 构建前按冻结 `executionScopes` 过滤 general-purpose 与 custom declarative subagent tools。两条 focused suites 分别 6/6、3/3 通过。
- 2026-07-19 Stage 4 Part 1 builder fixtures：14 个 wiring inputs 现以 `Omit<DeepAgentBuildInput, 'capabilityManifest'>` 表达 fixture 边界，并由 shared test helper 调真实 compiler 注入 manifest；custom MCP names 显式列出。wiring suite 14/14 通过。
- 2026-07-19 Stage 4 Part 1 verification：最终 finding repair 的 10-file aggregate 为 73/73；typecheck、strict unused、diff check 均通过。尚未完成独立复审，状态仍是 changed, unverified for commit gate。
- 2026-07-19 Stage 4 Part 1 independent Standards findings：full-stack/tool-retry fixtures 的 manifest 仍未由 compiler 生成；executor background tool registration 与 manifest inventory 各自维护。修复目标是 compiler-backed fixtures + manifest-driven executor filtering，保留 executor 的真实 tool factories。
- 2026-07-19 Stage 4 Part 1 executor repair design：移除 `backgroundTaskToolMode`/`readBackgroundTaskToolMode()` 第二合同；以同一 `PreviewStore` 构造 time + all + change 候选 tools，再只保留 immutable manifest 中登记的名字并去重。`requireWorkbenchSourceForBackgroundTaskWorkflow()` 继续独立约束 trusted origin。
- 2026-07-19 Stage 4 Part 1 Standards repair green：executor 现由 manifest name set 决定实际 background tool surface；full-stack/tool-retry fixtures 改用 compiler manifest。现有 ordinary/propose/change executor matrix 与两 fixture 共 4 files / 34 tests passed。
- 2026-07-19 Stage 4 Part 1 post-repair verification：12-file focused aggregate 102/102；typecheck、strict unused、IPC check、diff check 全通过。Standards findings resolved locally；Spec review still pending。

- 2026-07-17 Stage 3 恢复：当前未提交实现已横跨 task v4 schema、occurrence claim/lease、occurrence dispatch key、scheduler/reconcile、outbox projection 与对应回归测试；尚未运行本轮聚焦验证或独立 review，不能标记完成或提交。
- 2026-07-17 Stage 3 聚焦验证：occurrence、scheduler crash/reconcile、scheduler、outbox projector、task repository、agent runtime、migration/health/probe 共 9 个文件、60 个测试通过。独立 review 尚未开始。
- 2026-07-17 Stage 3 扩展验证：`pnpm typecheck` 通过；schema/history/session/runtime executor 聚焦集 3 个文件、29 个测试通过；`git diff --check` 通过。根 `AGENTS.md` 已确认被 `.gitignore` 忽略且未跟踪，不更新、不纳入本阶段提交。
- 2026-07-17 Stage 3 广泛验证：`pnpm test` 297 files / 1571 tests passed，进程退出码 0；过程中 `node-pty` 辅助进程打印既有 `AttachConsole failed`，未使测试失败。`pnpm build` 通过。
- 2026-07-17 Stage 3 独立 review 阻塞项：P0 为 scheduler restart 发生于 claim lease 未过期时，once occurrence 已使 `next_run_at=null`，但无 lease-expiry wakeup，最终永久停在 `claimed`；P1 为 active dispatched 存在时不推进/替换过期 pending occurrence，导致 0ms timer loop 且执行旧 occurrence，违背已确认 latest-only。附加 CAS 缺口：dispatch/failure 写入未绑定 claim owner/attempt，stale owner 可覆盖新 claim。必须加时序回归、修复、复审。
- 2026-07-17 Stage 3 review red tests：lease-expiry restart 和第二轮 overlap latest-only 均按预期失败；现有实现分别未调用 agent start、保留 00:05 pending occurrence，未产生 00:10 latest occurrence。
- 2026-07-17 Stage 3 review repair error：首次修复将 `pending` 保持为 `const` 后又赋值为 `undefined`，聚焦测试明确报 `Assignment to constant variable`；已改为 `let`，未重复原执行。
- 2026-07-17 Stage 3 review repair：scheduler 新增最低 claimed lease expiry 的一次性 reconcile wakeup；旧 pending overlap 在当前 due cron 出现时原子标 `skipped/overlap_coalesced_superseded` 并创建最新 pending；claim result 携带 owner/attempt，dispatch/start-failure 更新使用二者 CAS，stale owner 得到 `null` 不影响任务。5 个聚焦文件、36 个测试，`pnpm typecheck` 与 `git diff --check` 通过；待新独立复审。
- 2026-07-17 Stage 3 repair 扩展验证：完整 Stage 3 聚焦集 9 files / 63 tests passed；`pnpm check:ipc` 和 strict unused scan 通过。复审仍在进行。
- 2026-07-17 Stage 3 repair 全量验证：`pnpm test` 297 files / 1574 tests passed，exit 0；`pnpm build` 通过。测试期间仍有既有 `node-pty` `AttachConsole failed` 子进程输出，未改变成功退出码。等待复审。
- 2026-07-17 Stage 3 独立复审 #2：lease wakeup、latest-only、owner/attempt CAS 无新回归；仍缺 Stage 3 crash-point 验收：`startRun` 已在 agent DB 创建 run、task DB 尚未 mark dispatched 时的进程退出/双 DB reopen/lease expiry/dispatch-key re-use/terminal reconcile fixture。补齐前不能提交。
- 2026-07-17 Stage 3 crash-gap repair：新增持久 task/agent 双 DB fixture，模拟 `startRun` 已将 dispatch key 与 run 写入 agent DB、`markScheduledOccurrenceDispatched()` 前进程退出；reopen 后 lease 到期重领、同 dispatch key 查回同一 run、occurrence 关联并由 terminal reconcile 落为 completed。`scheduler-crash-consistency.test.ts` 2 tests passed。源码检索曾误指向不存在的 `tests/main/infrastructure/database-schemas.ts`，已改用实际 schema 路径，未重复错误命令。
- 2026-07-17 Stage 3 crash-gap 扩展验证：9 files / 64 tests passed；`pnpm typecheck`、`pnpm check:ipc`、strict unused scan、`git diff --check` 全部通过。最终独立 review 仍在进行。
- 2026-07-17 Stage 3 crash-gap fixture repair：改接真实 `AgentPluginRuntime` 后，首次断言在 scheduler timer 后立即读取到 `dispatch_pending`；runtime 执行由同一时刻新排的 0ms timer 驱动。fixture 增加一次 0ms fake-timer drain 后再断言 terminal/outbox，未改生产逻辑。
- 2026-07-17 Stage 3 crash-gap fixture repair attempt 2：单独 0ms drain 仍早于 detached scheduler `fire()` 内的 runtime timer 创建，状态仍为 `dispatch_pending`。改用 startRun-return deferred 确认 runtime 已排 timer，再 drain；不重复同一时序假设。
- 2026-07-17 Stage 3 crash-gap fixture repair attempt 3：deferred 后的同刻 0ms drain 仍不执行 newly-scheduled runtime timer。按 Vitest fake-timer 嵌套 timer 行为重新判定，改为推进下一个逻辑 tick（1ms），不再重复 0ms drain。
- 2026-07-17 Stage 3 crash-gap fixture runtime result：真实 runtime 已在 crash 前完成并写 durable outbox；restart scheduler link 后立即调用 reconcile，因此 occurrence 已是 `completed`，不是中间 `dispatched`。fixture 改为断言这一更强的 terminal projection，再验证重复 reconcile 幂等。
- 2026-07-17 Stage 3 crash-gap real-runtime verification：`scheduler-crash-consistency.test.ts` 2 tests passed。fixture 不再手写 agent SQL 或 dispatch-key lookup；使用持久 `AgentSessionRepository` 与真实 `AgentPluginRuntime.startRun()`，并检查 terminal outbox、双 DB reopen、agent unique dispatch key、scheduler terminal projection。
- 2026-07-17 Stage 3 final pre-review verification：9 files / 64 tests、typecheck、IPC、strict unused、diff check 均通过；`ROC_SMOKE_TARGET=dist pnpm smoke:electron` exit 0。smoke 的 Node deprecation warnings 未影响退出码。
- 2026-07-17 Stage 3 独立复审 #4 blocker：plan 要求四个 crash point 的 scheduler restart fixture；当前仅覆盖 start/link gap，缺 claim 后未 start 的持久 scheduler restart，以及 terminal/outbox projector 前 crash 的持久 restart/projector cursor replay。repository-only lease fixture、memory-only outbox fixture 不能替代。补齐前不可提交。
- 2026-07-17 Stage 3 four-crash repair：`scheduler-crash-consistency` 现有四个明确流程：claim/start/link 顺序、持久 claim 后未 start restart、真实 Agent runtime start 后未 link restart、terminal outbox 持久化后未 task projector restart/cursor replay。后两条使用双 DB reopen；terminal case 检查 actual outbox、occurrence terminal 与 replay idempotence。crash suite 4 tests passed，待独立复审。
- 2026-07-17 Stage 3 review 补充 blocker：projector 已有重复/restart 与旧 run 投影测试，但没有乱序/sequence gap 回归。需锁定 sequence 2 先到时拒绝、cursor/task DB 不变，随后 1→2 正常投影；不可只依赖生产 `task_agent_outbox_sequence_gap` 检查。
- 2026-07-17 Stage 3 projector order repair：新增 sequence 2 先到必须抛 `task_agent_outbox_sequence_gap`、cursor 仍为 0、task projection 未变，随后 1→2 以 `appliedCount=2` 正常提交的回归。`agent-outbox-projector.test.ts` 6 tests passed；待独立复审。
- 2026-07-17 Stage 3 final review blocker：terminal outbox crash fixture 虽为双 DB 重启，但直接调用 repository projector，未覆盖 `createTaskPlugin().initialize()` 的 production startup replay/batch loop/boundary。改为以 fresh persistent plugin context 初始化 task plugin，断言 occurrence/cursor；第二次 plugin restart 断言无重复投影。
- 2026-07-17 Stage 3 startup projector repair：terminal outbox fixture 已用 fresh `createTaskPlugin().initialize()` 的持久 DB context 完成首次 startup replay 和 cursor 写入；第二次 fresh plugin 初始化仍保持同一 cursor/terminal occurrence。crash suite 4 tests passed，待独立复审。
- 2026-07-17 Stage 3 post-startup-repair verification：完整 Stage 3 聚焦集 9 files / 67 tests passed；typecheck、IPC check、strict unused、diff check 均通过；最终 review 仍在进行。
- 2026-07-17 Stage 3 final review/validation：独立 review #6 无 blocker。全仓 `pnpm test` 297 files / 1578 tests passed，`pnpm build` 通过，Electron smoke、IPC、strict unused、diff check 全通过。`node-pty AttachConsole failed` 和 Node deprecation 均为 exit 0 的既有环境输出。`plan.md` 是用户未跟踪验收源，保留但不纳入提交。
- 2026-07-17 Stage 3 commit：`bc87156 feat(task): make scheduled occurrences durable`。Stage 4 的实现前 Gate 仍未确认：interactive 是否逐次 shell approval、background 是否仅在 task 创建时 durable pre-authorization；workspace isolation 若要求必须走 OS sandbox，不能用 cwd guard 替代。

- 工作树当前仅有用户提供、未跟踪的 `plan.md`；代码分支为 `main`。
- `.codegraph/` 存在；代码定位必须优先使用 CodeGraph。
- Stage 0 目标是表征测试，不改变运行行为。
- `plan.md` 指定的 shell scope 与 scheduler misfire 属于产品行为 Gate，分别在 Stage 4、Stage 3 切换前确认。
- `AgentPluginRuntime.startRun()` 只以 `run.id` 加入 `activeRuns`，未见 per-thread lease；Stage 0 可先用受控 executor 固定复现同 thread 双启动。
- `AgentPluginRuntime` 同步等待 EventBus；`EventBus.publish()` 在 subscriber 失败后抛出 `AggregateError`，这是 terminal 投影复现的真实路径。
- `TaskScheduler.fire()` 先 `startRun()`，后 `runNow()` 记录 task 关联；Stage 0 crash fixture 应固定这个间隙。
- `completeRun()` 当前在 `agent.run.completed` publish 前移除 `activeRuns`；subscriber failure 不会把 persisted completed 覆盖为 failed。`plan.md` 对这一条的“当前问题”已过时，Stage 0 应保留回归 characterization，而非伪造旧故障。
- 现有 runtime fixture 集中在 `runtime-executor.test.ts`、`runtime-streaming.test.ts`、`runtime-approval.test.ts`；计划指定的新 Stage 0 文件尚不存在。
- shell 现有测试位置为 `tests/main/plugins/runtime-tools/shell-adapter.test.ts`，不是计划中的旧路径；新 cancellation fixture 可按计划创建。
- Step 3 官方/本机契约：interrupt 必须使用 persistent checkpointer 与稳定 `configurable.thread_id`；重建后的 agent 用同一 thread 和 `new Command({ resume })` 恢复。现有 `sqlite-checkpointer.test.ts` 仅验证 saver DTO，尚无真实图 restart fixture。
- Step 3 已用真实 LangGraph graph 验证：approval 原生 `interrupt()` 与 Roc `createAskUserTool()` 都能在新 graph/new saver instance 中用相同 thread resume；不依赖 provider 或网络。
- Step 3 review 修正：approval fixture 改走真实 `createDeepAgent({ interruptOn })`；Deep Agents 1.10.7 通过原生 `humanInTheLoopMiddleware` 生成 approval interrupt。
- Step 3 review 通过：文件 SQLite reopen、new saver/new graph/new Deep Agent、same thread、native HITL 和 Roc ask_user 均已覆盖。
- Step 4 当前 scheduler 无 durable occurrence/claim、crash hook 或 terminal projection；可直接证明的当前路径是 `startRun -> recordScheduledTaskRun -> markBackgroundTaskFired`。真正四个 crash/restart fixture 需要 Stage 3 occurrence 状态机落地后验证，不能由现有 timer 伪造。
- Step 4 review：scheduler 顺序基线符合当前源代码且无生产行为改动；它不是完整 crash recovery 证据，Stage 0 Gate 不能因此宣告完成。
- Step 5 当前 `ChatRunEventQueue` 没有 queue depth/high-water 观测，`AgentRunEventLog` 没有 event row cap；因此无法从现有公开或测试接口诚实记录计划要求的 queue high-water。可在不改生产代码时测 10k/100k 吞吐、SQLite rows、replay time，但高水位需 Stage 2/6 的 instrumentation。
- Step 6 当前 Deep Agent stream 接收 run `AbortSignal`，但 `createRocWindowsCommandTool()` 的 adapter contract、`ShellExecutionService` 和 `HookCommandRunner` 均未接收该 signal；`WebReadService` 仅有自身 timeout 的 `AbortController`。因此 shell/web/hook 的 graph-cancel 与 Windows child-tree regression 只能随 Stage 4 的统一执行合同实现，不能用脆弱的源码形状断言伪装为当前完成。
- Stage 1 的真实持久化 owner 是 `AgentSessionRepository.createTaskRun()`：它在同一 agent DB 事务创建 thread/run/user message。Task plugin 只读取该 DB 作工作台投影。现状没有把 `capabilityPreview` 写为 `context_manifest`，因此 Electron smoke 找不到审计卡。修复必须和 snapshot 同事务完成，不能把 task subscriber 变成第二事实源。
- 2026-07-15 恢复核对：当前 WIP 的 `AgentSessionRepository.createTaskRun()` 已在创建 run/user message 的同一事务插入 `context_manifest`。但 Electron smoke 仍报 `No context_manifest event found after chat submit`；下一步应追 `agent_events` 的读取、replay 和 smoke 诊断投影链，不能再增加平行写入。
- 2026-07-15 恢复核对：session catchup 无未同步上下文；当前工作树包含 Stage 1 的未提交 WIP，`plan.md` 仍是未跟踪的用户提供验收源。
- Stage 1 Gate：不得把 preview、executor、subagent、HITL 或 effect policy 的授权计算分散保存；验收是给定 `runId` 仅从 agent DB 重建执行请求，且所有消费者共享一个 manifest hash。当前只处理这个 contract 和由它导致的 smoke 回归，不提前进入 Stage 2。
- CodeGraph 当前调用链：`startRun()` 创建 snapshot 后用其生成 normalized request，并把同一 snapshot 传到 `executeRun()`；executor contract 已只接收 `snapshot`。`createTaskRun()` 校验 preview/snapshot manifest hash 相同后，在同一事务写 run、user message、`context_manifest`。需确认 smoke 未观察到事件的实际消费链及 WIP 是否仍有其他授权性重算。
- 回归基线：`rtk pnpm test -- <path>` 会把额外 `--` 传给 Vitest，导致路径过滤失效；聚焦测试必须用 `rtk pnpm exec vitest run <path>`。误跑的全套暴露 Stage 1 回归：agent schema 已为 3 而基础设施健康测试仍断言 2；agent plugin 调 workspace capability 时未声明依赖；runtime hooks fixture 发送了非 workbench 来源的 `workspacePath`，被新来源合同拒绝。
- 根因定位：`createAgentPlugin()` 无条件配置 `workspaceProvider` 并在 run start 调 `workspace.getCurrent`，但 `resolveDependencies()` 只在非 mock deep executor 场景添加 `@roc/plugin-workspace`。依赖应与实际 capability 调用一致。`runtime-hooks` 的 `workspacePath` 是模拟 workbench workspace，应显式加 `taskSource: 'workbench'`，而非放宽生产来源合同。
- Smoke 推断：`createTaskRun()` 对 user message 与 `context_manifest` 使用同一个 timestamp 和数据库事务；task snapshot 同样直接读取 `agent_events`。若同一 smoke 已读取到该 run 的 user event，却读不到后写入的 manifest，当前源码不可能产生该持久化状态。先验证 smoke 的 Electron 运行目标是否为未重建的 `dist`；不在 repository 增加补写或双写。
- Smoke target 机制：`pnpm smoke:electron` 直接启动 smoke 脚本；未显式 `ROC_SMOKE_TARGET=dist` 时优先使用已存在的 `release\\win-unpacked\\Roc.exe`，否则使用 `dist/main/index.js`。该 smoke assertion 是当前 WIP 新增，但 target 可为旧构建，足以解释其与当前 repository 事务矛盾。正确验证顺序是先 build，再明确 `ROC_SMOKE_TARGET=dist` 运行。
- 现场确认：`release\\win-unpacked\\Roc.exe` 和 `dist\\main\\index.js` 均存在，且 `ROC_SMOKE_TARGET` 未设置。因此此前 `pnpm smoke:electron` 实际优先运行已存在 packaged target，不是当前 source；Stage 1 smoke 必须先 build 后显式 `ROC_SMOKE_TARGET=dist`。
- Stage 1 dist smoke：`context_manifest` 已出现在 task snapshot，原 `No context_manifest event found after chat submit` 已验证为旧 target 假阳性。新的独立失败：chat composer `Enter` 后输入仍留在框内、`[data-testid="chat-transcript"]` 为空，超时等待 `Smoke Provider 已生成首轮回复。`。先定位 renderer submit 事件；不修改 manifest/repository。
- Composer source：textarea 的裸 `Enter` 已调用 `preventDefault()` 和 `submitComposer()`；send button 同样调用该函数。现有 renderer tests 覆盖上层 queued task 的 keyboard submit，但没有覆盖真实 `ChatComposer` 的多行 `Shift+Enter` 后裸 Enter。因此下一步先检查 submit disabled/state 与 smoke artifact，再决定是 renderer 实现还是 smoke focus/interaction contract。
- Composer wiring：`ChatComposer` 将 `submitComposer()` 直接绑定到 `ChatView.submitCurrentInput`。smoke 在 submit 前已验证 send button 未 disabled，但其 timeout debug 未包含该 pre-submit evidence；需检查 `submitCurrentInput` 的 guard，以及实际 `document.activeElement`，不能仅凭 input 保留文本判断键盘事件未到达。
- `submitCurrentInput` 若进入 success path 会在等待 assistant 前立即清空 input；此次 input 未清空，故最终 Enter 未触发 textarea handler，而非 provider response 延迟。现有 `.artifacts\\wave1` 是旧产物，本轮 smoke 未落入可用 failure artifact；在 smoke debug 中补焦点与 pre-submit evidence，再复现一次。
- 焦点复现证据：final key 前 `activeElementTestId` 为 `chat-input`，输入可编辑、send button 未 disabled，`submitCurrentInput` guard 不成立；全局 `page.keyboard.press('Enter')` 仍未触发提交。此处用 locator-targeted `press('Enter')` 保持真实 textarea key path，并避免 Playwright/CDP 全局键盘焦点漂移；不改 production behavior。
- locator-targeted Enter 同样失败，故不是 focus/CDP 全局键盘问题。未清空 input 且无该 typed prompt 的 agent event 表明 `onSubmitChatTask` 未返回成功，或 run start 被 IPC/main 拒绝。下一步应直接采集 renderer error 和 IPC result；停止修改 keyboard path。
- 根因：`use-app-task-runs.ts` 的普通 chat request 发送 `taskSource: null` 与 `workspacePath: currentWorkspacePath`；Stage 1 runtime 正确以 `agent_workspace_path_source_invalid` 拒绝该裸 path。普通 chat 应不传 `workspacePath`，由 main 的 plugin-scoped `workspaceProvider` 解析当前 workspace；不能为兼容 renderer 输入放宽 runtime policy。
- Renderer coverage：`app-shell.test.tsx` 当前在普通 chat 与 plan-chat 两处断言 `taskSource: null` 仍携带 `workspacePath`，因此必须随 production request 一起更新。`startTaskRun()` 的 workbench task path 继续携带显式 workspace path，不能被本次改动触及。
- 修复后 dist smoke 已越过 composer submit 与 manifest assertion，新的失败为 `No provider agent_update event found after chat submit.`。这符合 Stage 1 未完成项：`createTaskRun()` 当前事务只写 user message 与 context manifest，未写现有 consumer 所需的 canonical run start/provider event。先追 `agent_update` 的旧生产路径与 payload contract，不改 smoke 降级。
- `agent_update` 当前由 Task plugin 的 `agent.run.started` EventBus subscriber 写入，而不是 `createTaskRun()` 的 agent DB 事务；runtime publish 在创建后发生。Stage 1 需要把 canonical start/provider timeline event 同 run snapshot/user message 原子持久化，并防止 Task projector 继续重复写入。先读 projector payload/ownership和对应 tests。
- 精确缺口：`TaskRepository.recordAgentRunStarted()` 在 `agent_runs` 已存在时立即返回；Stage 1 repository 先创建 run，导致 EventBus task projection 不再插入 `agent_update`。把 running/provider/model timeline event 直接加入 `createTaskRun()` 事务可恢复现有 consumer 合同，同时避免重复写入。
- 兼容 payload：legacy task projector 的 start event 是 `agent_update` `{ status: 'running' }`；不要借修 smoke 擅自扩展 timeline payload。Stage 1 repository 应原子写同形 event，repository test 从三项初始事件更新为 message、context manifest、agent update、后续 assistant block。
- 当前 dist smoke 已证明 start event 存在，但 `taskProviderUpdateStored=false`：smoke 的 provider timeline contract 需要 `providerId` 与 `modelId`。将 immutable snapshot 的 model identity 写入同一 start event；这是 Stage 1 明确的 provider/model recovery contract，不是 UI 特判。另有独立 `naturalLanguageTaskCreated=false`：task 已创建但缺 `background_task_created` event，待 provider event green 后单独追踪。
- 最新 dist smoke 已完成全链路并收敛为两个失败检查：`taskProviderUpdateStored` 与 `naturalLanguageTaskCreated`。前者说明 start event identity 与 smoke expected provider 不一致，后者说明 background task 已存在却没有 `background_task_created` timeline record；两者均须先查实际 writer 与 predicate，不能根据名称补写。
- Writer/predicate 对齐：`createBackgroundTask()` 确实写 `background_task_created`，但 payload 只有 `taskId/status`；smoke 按 `goal` 匹配，故 task 已创建仍判失败。需在该 event 写 task goal。provider smoke 则要求 terminal `agent_update` 包含 `finishReason:'stop'`，start event 不足；另行检查 completed projector。
- Smoke predicate detail：`taskProviderUpdateStored` 要 terminal event 的 `providerId`、`modelId`、`finishReason:'stop'`，不能由 start event 满足；`naturalLanguageTaskCreated` 的缺口只在 created audit payload 少 `goal`。两个生产路径独立，先用 task repository test 固定 created event goal，再追 terminal projector。
- 2026-07-16 会话恢复：catchup 表示完成 terminal `agent_update` 与自然语言 task audit 的 smoke 修复后，正收尾显式 `/skill` 的 immutable manifest 合同：selected 与 explicit skill identity 进入 manifest；executor 只读 snapshot、不会运行时重新列技能；显式技能同样进入 `/skills/` backend 白名单，并在 manifest 编译阶段拒绝 disabled/not-found/invalid 项。该变更的 8 files / 63 tests 已通过；后续 `pnpm typecheck` 的结果必须重新回收确认。
- 2026-07-16 验证：无残留 `typecheck` 进程；重新执行 `rtk pnpm typecheck` 通过。覆盖本次变更的 22 个 Vitest 文件（数据库 migration/rebuild、manifest、executor、runtime、task repository 与 renderer）共 137 tests 通过。
- 2026-07-16 完整验证：`pnpm check:ipc` 确认 generated files current；fresh `pnpm build` 成功；`ROC_SMOKE_TARGET=dist pnpm smoke:electron` exit 0，明确验证了刚重建的 dist。smoke 输出仅有 Node deprecation warnings，不影响成功退出。
- 2026-07-16 review 静态证据：`startRun()` 编译 capability preview、创建 snapshot seed，再由 `createTaskRun()` 在同一事务写 run、user message、`context_manifest`、canonical start `agent_update` 与 `run_started` event；随后重新从 repository 读取 snapshot 再构造执行请求。`getRunExecutionSnapshot()` 会对缺失、JSON 无效、schema/identity 不匹配的 snapshot quarantine 并失败。`git diff --check HEAD` 通过；等待独立 standards/spec review 对完整差异作最终判定。
- 2026-07-16 broad verification：`pnpm exec tsc --noEmit -p tsconfig.json --noUnusedLocals --noUnusedParameters` 通过；`pnpm test` 为 292 files / 1522 tests 通过。测试进程后输出四次 `node-pty` `AttachConsole failed`，但 Vitest exit 0，属于既有 Windows teardown 噪音，不能作为失败或本次修复证据。
- 2026-07-16 双轴 review 已确认两项当前 Stage 1 合同缺口：(1) `resolveRunOrigin()` 仅看 `workflowHint`，普通 chat 可被标为 `workbench_creation`，而 scheduler 使用 `taskSource: 'workbench'` 仍会落为 `manual_task_run`；由于 executor snapshot-only，这会冻结错误的授权来源。(2) compiler 在 `manifest.skills` 加入 explicit skills，却只将 requested selected skills 返回为 `skillCards`；repository 持久化该不完整卡片列表，导致 explicit skill 可执行但审计卡缺失。review 建议的 Stage 2 outbox 与完整 subagent safety stack 属于后续 stage，不提前实现。
- Run-origin 回归定位：当前 `ChatStartRunRequest.taskSource` 只表达 `'workbench' | null`，scheduler 也复用了 `'workbench'`；必须增加明确的 trusted scheduler value，并同步更新 renderer/workbench、scheduler 和测试 helper 中用 workflow hint 猜 origin 的逻辑。`resolveRunWorkspace()` 已把非-workbench 的 `workspacePath` 作为来源错误拒绝，故 scheduler 的 trusted source 应延续该显式来源检查而非绕过它。
- Snapshot rehydration 还会把所有 `mode: 'task'` 的 request 一律还原为 `taskSource: 'workbench'`。新增 `background_schedule` 来源时必须使 `createChatStartRunRequestFromSnapshot()` 保留该来源；否则 initial run 虽冻结正确，resume/metadata request 又会丢失 provenance。
- Review fix：`taskSource` 新增 `background_schedule` 并只允许该来源冻结为同名 `runOrigin`；workflow hint 必须同时有 `taskSource: 'workbench'`，否则在持久化前以 `agent_workflow_hint_source_invalid` 拒绝。scheduler、workspace source validation、new-thread kind 与 snapshot rehydration 均同步到该来源。compiler 的 `skillCards` 现在从完整 `manifestSkills` 生成，故 selected 与 explicit skills 均写入同一 `context_manifest` audit card。新增/扩展的 3 files / 22 tests 已 green；完整再验证仍待执行。
- Post-review focused suite 的两个失败均是 fixture 仍构造旧 provenance：executor unit test 需要 frozen `workbench` snapshot，runtime tool-block test 需要 workflow hint 的 workbench source。仅更新这两个测试输入后，Stage 1 focused 集合为 23 files / 144 tests 通过。
- Post-review full verification：strict unused scan、`pnpm check:ipc`、fresh `pnpm build`、明确 `ROC_SMOKE_TARGET=dist` 的 Electron smoke 与 `pnpm test` 全部通过；全量为 292 files / 1524 tests。full Vitest exit 0 后仍有已知 Windows node-pty `AttachConsole failed` teardown 噪音，非本次回归。
- 2026-07-17 Stage 2 entry：用户要求继续。Stage 0 的 Step 5 明确依赖 Stage 2 queue/event instrumentation，因此开始 Stage 2 preflight；不提前实施 Stage 3 occurrence、Stage 4 cancellation 或未确认的产品 Gate。Stage 2 authoritative spec 为 `plan.md` 第 223–303 行。
- Stage 2 native contract：Deep Agents 已是 harness，Roc 只实现其自身的 durable run/outbox/projector 边界；`thread_id` 保持 checkpoint cursor，agent DB transition/outbox 负责外部可见状态与幂等 side effect。恢复依赖稳定 thread、persistent checkpointer 和 operation key，不能用 prompt 或 EventBus subscriber 充当授权/状态事实源。
- Stage 2 current path：`completeRun()` 分别调用 `updateRunStatus`、`clearPendingInterrupt`、`recordSessionMessage`、两次 `recordEvent`，随后再同步 `EventBus.publish`；`failRun()` 也先清内存/interrupt、再单独更新状态和发布。Task plugin 只订阅 EventBus，再更新 task DB，故 observer/projection 仍能影响运行控制流。`AgentRunEventLog.recordRunEvent()` 仍以 `MAX(sequence)+1` 分配 sequence。
- Task projector baseline：`recordAgentRunStarted()` 遇到 agent DB 已有 run 直接返回，started timeline 已由 Stage 1 repository 原子写入；`recordAgentRunCompleted()` 仅更新 background task 状态。Stage 2 不应重建 agent history 投影，而应把 task-DB 副作用改为 agent outbox 的幂等消费。
- State baseline：`agent_runs.state_version` 已在 Stage 1 schema 中写初值 `1`，但 `updateRunStatus()` 未带 expected status/version，且 transaction 只写 run/thread 行。`markRunInterrupted()` 同样不做 CAS。现有 migration ledger 可安全追加 agent DB schema；第一实现步骤应复用该字段而非新增平行 state owner。
- Schema baseline：agent migrations 当前止于 v3；没有 outbox/cursor 表。`agent_run_events` 的 `(run_id, sequence)` primary key 可拒绝重复，但 allocator 仍 `MAX+1`；`agent_events.sequence` 仅有索引，没有唯一约束。Stage 2 migration 应添加状态/transition 所需约束、outbox/cursor 与安全 append allocator，不重写 v1–v3。
- Test baseline：`run-event-log.test.ts` 仅覆盖单线程的 1→2 replay；需要新增并发 append/allocator contract。terminal characterization 目前只保证 completed 先落库、subscriber rejection 不再覆盖为 failed；Stage 2 需改为 terminal transaction 完成后 best-effort notification，并用 outbox projector 覆盖其副作用。
- Repository/migration test baseline：`session-repository.test.ts` 覆盖 create/interruption/snapshot quarantine，但没有 transition CAS、illegal transition 或 terminal atomicity。`database-migrations.test.ts` 明确锁 agent v3，Stage 2 v4 migration 必须追加并扩展该 test；不能修改已有 migration。`plugin-run-states.test.ts` 仍以 EventBus synthetic events 表征 task snapshot，Stage 2 要保留其 renderer-facing contract，同时增加 outbox duplicate/restart projector fixture。
- Agent/task ownership detail：Task workbench history直接读 agent DB 的 threads/runs/events；`recordAgentTaskEvent()` 当前无写入，EventBus task-event subscriber 不是真实 history owner。Stage 2 projector只需消费 agent outbox 中的 task-DB side effects（如 background task last-run status），不得复制 agent timeline。`AgentTaskHistoryReader` 也有 `MAX+1` sequence writer，须与 repository allocator 一并收敛。
- Lease detail：`hasActiveThread()` 仅检查未 archived thread，不检查 active run；同 thread 再 start 会创建下一 run number。Stage 2 thread single-flight 必须以 agent run status/lease 实现，不能复用该 helper 的“thread exists”语义。
- Red evidence：Stage 2 first-slice contract 如预期失败——migration ledger 仅到 v3，`transitionRun()` 缺失，同 thread 第二 active run 未冲突。修复范围锁为 migration v4 + repository；runtime仍使用旧更新路径，符合 migration sequence step 1。
- First-slice green：agent v4 新增 `agent_run_leases`；`createTaskRun()` 在同一 agent DB transaction 获取 lease，并将现有 active run 与唯一 lease constraint 映射为 structured `thread_run_conflict`。`transitionRun()` 实现 expected status + `state_version` CAS，非法 transition 与 stale update 分别返回 `agent_run_transition_invalid` / `agent_run_transition_conflict`。terminal old-path updates 和 snapshot quarantine 同时释放 lease。migration/repository focused suite 19 tests 通过；runtime 尚未迁移到 CAS API。
- Concurrency regression update：`runtime-thread-concurrency.test.ts` 的旧 characterization 直接断言同 thread 两个 executor 均启动。v4 lease 使第二个 `startRun()` 在 executor 调度前以 `thread_run_conflict` 拒绝；该测试现应锁定单 executor 与首 run 保持 `waiting_next_turn`。CodeGraph 同时确认 `transitionRun()` 目前只有 repository test caller，运行时 CAS/outbox 切换仍属于后续 slice。
- Impacted-suite correction：首次将并发 fixture 改为冲突断言时，首 run 暴露 `createTaskRun()` 一个残留的 `hasActiveThread` 标识符；该变量已在本次 lease 实现中改名为 `hasExistingThread`。同时 database fast-probe/health 的 schema ledger 断言仍为 agent v3，均应随 v4 migration 定点更新。
- Terminal/outbox design boundary：agent DB 现有 `agent_run_events` 与 `agent_events` 是不同 timeline；terminal transaction 必须原子写 terminal run、assistant facts、agent event、terminal run-event 与 agent outbox。task DB 是 consumer，cursor 必须在 task DB 内与投影副作用同事务提交；不能把 task workbench history复制到 outbox，因为它直接读取 agent DB。现有 EventBus completed/failed subscription 仅更新 background task projection，暂留作 Step 3 shadow baseline。
- Completed terminal transaction green：agent v5 `agent_outbox` 使用 SQLite `INTEGER PRIMARY KEY` sequence；task v3 新增 `task_agent_outbox_cursors`。`completeRunAtomically()` 在同一 agent transaction CAS terminal status、释放 lease、清 pending interrupt、写 assistant session/message event、terminal `agent_update`、`run_completed` replay event 和 completed outbox。其 runtime adoption仍按 Stage 2 Step 4 延后，避免与当前 EventBus 双写。
- Failed terminal transaction green：`failRunAtomically()` 以相同 CAS/lease/pending cleanup transaction 写 `error` history event、`run_failed` replay event 与 failed outbox；terminal outbox当前允许的事件类型恰为 completed/failed，对应 task projection的既有 success/pause side effect。
- Task projector green：`AgentTaskHistoryReader` 只读 agent outbox，并对 payload/event type 做显式校验；`TaskRepository.projectAgentOutboxEvents()` 在 task DB transaction 内按严格连续 sequence 更新 background task 与 durable cursor。completion shadow fixture 将当前 EventBus projector与新 outbox projector的任务状态投影作比较；duplicate/restart 与 failure pause 均已覆盖。projector尚未接入 plugin lifecycle，避免与旧 EventBus path 双写，待 runtime terminal switch 后一次切换。
- Runtime/projector switch green：`completeRun()`/`failRun()` 已改为读取当前 transition state 后调用 terminal transaction；runtime EventBus publication统一为 best-effort，因此 started/completed subscriber rejection不能阻断或改写 outcome。task plugin initialize 先 drain agent outbox，completed/failed notifications仅触发 drain，旧 payload projector不再作为事实源。终态 chat event不再由 `runEventLog` 二次写入；其 `createdAt` 保持既有 run-start semantics，持久化与通知一致。
- Sequence cursor implementation：agent v6 为 per-thread `agent_events` 与 per-run `agent_run_events` 分别建立 cursor，且在 migration 中从历史最大 sequence 回填；新的 agent event unique index把重复 sequence 作为显式持久化错误。所有生产 writer（session repository、run event log、task background history）均在 SQLite transaction 内读取/递增 cursor 再插入事件。v6 新外键要求 thread deletion先删 lease/outbox/cursor；独立 run-event test亦必须先持久化其 run。
- Sequence validation：旧 task-plugin fake agent fixture已改为同样持久化 terminal outbox与`error` failure event，因而能覆盖真实 projector而非只靠 EventBus。agent/task focused suite 33 files / 181 tests已通过；`MAX(sequence)` 仅保留在 v6 一次性历史 cursor backfill，不再出现在生产 append path。
- Queue slice green：`ChatRunEventQueue` 现有显式 `maxEvents`、`highWaterMark` 和当前深度；相邻同 block text delta 合并，结构事件不合并。满队列时以 `chat_run_event_queue_overflow` 失败 stream，而非无界增长。queue unit 2 tests 与 typecheck 通过；startup reconciliation 仍未实现。
- 2026-07-17 恢复：Stage 2 的已写 WIP 覆盖 CAS、terminal transaction、outbox projector、event cursor 与 bounded queue；当前唯一未完成生产缺口是 startup reconciliation。验收需补 restart/non-terminal run 的可解释终态测试，并补齐 plan.md 要求的 queue stress/backpressure 证据。继续保持 Stage 2 边界；不进入 Stage 3 occurrence 或未确认产品 Gate。
- Reconcile 调用点已定位：`createAgentPlugin().initialize()` 只 apply schema、创建 `AgentPluginRuntime`、注册 capability，当前没有恢复入口。恢复不能默认自动执行旧 `running` snapshot，避免重复 effect；最小安全合同是 atomically mark `dispatch_pending`、`running`、`recovering`（以及尚未调度的 `waiting_next_turn`）为 `interrupted`，并把合法、持久化的 `waiting_user` interrupt hydrate 回 runtime 以允许显式 resume。损坏 snapshot 或缺 pending interrupt 的 `waiting_user` 也必须落为 `interrupted`，避免 silent orphan。
- 现有 `resumeRun()` 已通过 `resolvePendingInterrupt()` 回读 repository，因此启动时只需 hydrate persisted pending interrupt；无须复制 interrupt state。reconcile 应在 capability 注册前完成，使首个 IPC request 看到持久化后的状态。
- 更正：`resolvePendingInterrupt()` 已在首次 resume 时懒加载并缓存 repository interrupt，reconcile 不需要 hydrate。保留 `waiting_user` 即可；只把 `waiting_next_turn`、`dispatch_pending`、`running`、`recovering` 统一落为显式 `interrupted`，释放其 lease，绝不自动重放。缺失或损坏 interrupt/snapshot 仍由既有 resume/quarantine 合同显式失败，不建立第二条恢复路径。
- 现有 `runtime-approval.test.ts` 已证明：新 runtime 可从 repository 直接恢复 `waiting_user` 后显式 resume。`ChatRunEvent` 没有无-HITL payload 的 terminal interrupted 事件，task outbox 目前只投影 completed/failed；reconcile 必须同时设计真实 terminal outbox 投影，不能只更新 agent row 后留下 task 运行中。
- Stage 2 一致性缺口：现有 snapshot quarantine 把 non-terminal row 直接写为 `interrupted`，但未递增 `state_version`、未走 durable terminal outbox。reconcile 与 quarantine 必须复用同一 CAS terminal writer：更新 run/thread、删 lease/pending interrupt、写 `error` timeline、写 `run_failed` replay event 与 outbox。agent run 语义仍为 `interrupted`；task projection 使用既有 failed terminal outbox 并携带明确 restart/quarantine code。
- 进一步核对：task projector 已把既有 `run_failed` outbox 映射为 background task `paused/failed`，因此 restart interruption 可复用该严格 payload，不必扩大 shared event union。Stage 2 仍有旧 `updateRunStatus()` 调用（interrupt、resume、retry、cancel）；其中 cancel 属于 Stage 4，HITL/retry 属于当前 CAS 合同，必须先全量定位后统一改为 expected status/version transition，不能只补 startup 形成两套状态语义。
- Red gate：新增 startup reconcile repository fixture 失败于 `repository.reconcileStartupRuns is not a function`；同文件其余 14 tests 通过。该 fixture 固定四种 process-interrupted state、waiting-user preservation、lease release、state version、replay、outbox 与 idempotency。
- Startup reconciliation green：`AgentSessionRepository.reconcileStartupRuns()` 将 `waiting_next_turn`、`dispatch_pending`、`running`、`recovering` 用 CAS terminal writer 落为 `interrupted`，并写同形 `run_failed` replay/outbox；有可解析 pending interrupt 的 `waiting_user` 保留，损坏 snapshot 由同一 writer quarantine。plugin initialization 在 capability 注册前调用该方法。repository 15/15 与 plugin 15/15 通过。
- Queue/stream path：`AgentDeepAgentExecutor` 把 text/tool/hook/context event 同步 push 到 bounded queue；overflow 令 async iterator reject，runtime 随后走既有 terminal failure writer。当前 queue 在 coalescing 前检查 capacity；需先合并再判满，避免可合并 text 被误拒。`AgentRunEventLog` 仍无 row cap；采用固定 10,000/run 上限，超过时显式 `agent_run_event_log_capacity_exceeded`，让 runtime 终止而非删历史制造 replay gap。
- Backpressure red：新 10k/100k fixture 的 queue stress 已通过现有 coalescing，但 timeline test 因 `agentRunEventLogMaxEvents`/capacity 不存在而零事件 replay；同时补充 `maxEvents: 1` 的同 block second delta，锁住必须 coalesce-before-capacity 的顺序。
- Backpressure green：queue 先 coalesce 后检查 capacity；100k adjacent text 保持 high-water 1，10k structural producer 在 1,000 停止。`AgentRunEventLog` 固定每 run 10,000 行，越界抛 `agent_run_event_log_capacity_exceeded` 且不消费 sequence；5 次 10k replay 的 P95 小于 1 秒。fixture 2/2 通过。
- CAS 收敛路径：保留 repository 直建 run 的 `waiting_next_turn` 兼容历史/fixture；runtime 在 publish 前 CAS 为 `dispatch_pending`，timer 在实际 executor 调用前 CAS 为 `running`。HITL 以 `running -> waiting_user` 和 resume `waiting_user -> running` 原子写 pending row，retry 走 `running -> recovering -> running`。cancel 改 CAS，但完整 cancel terminal projection 留 Stage 4。
- CAS red：HITL fixture 证明当前 `markRunInterrupted()` 仅更新 status，stateVersion 仍为 1；`resumeRunAtomically` 尚不存在。并发 runtime fixture 在 executor 已启动时仍见 `waiting_next_turn`。这三项为本轮实现回归锚点。
- CAS green：runtime start 先持久化 `dispatch_pending`，timer 在 executor 前 CAS 到 `running`。`markRunInterrupted()` 和 `resumeRunAtomically()` 分别原子写 `waiting_user`/pending 与 `running`/clear pending；retry 明确经过 `recovering`，cancel 使用 CAS transaction。`updateRunStatus()` 和唯一 stale fixture 调用已删除；runtime 12/12、approval 6/6、repository 15/15、concurrency 1/1 通过。
- Stage 2 focused run（30 files/156 tests）失败 3 项：database health 与 fast probe 仍断言迁移前 agent v4/task v2；当前 schema 正确报告 agent v6/task v3。streaming fixture 仍逐 chunk 断言，和 Stage 2 queue text coalescing 合同冲突；应锁最终字符序列与非 text 结构事件，不锁 producer chunk 边界。
- Regression fixes green：health/fast probe fixtures已只更新 ledger 版本为 agent v6/task v3；streaming fixture改为完整文本序列而非 transport delta 边界。`database-fast-probe` 2/2、`database-health` 3/3、`deep-agent-executor-streaming` 4/4 通过。
- Typecheck blocked：`session-repository.ts` 的 restart/quarantine paths 将 nullable SQLite provider/model 输入传给仅收 string 的 guard；新 reconcile test 写成过时 approval payload flat shape。均是当前 Stage 2 改动，先做显式 nullable guard与测试 payload 修复，再重跑。
- Independent Stage 2 review results: (P0) queue overflow does not stop producer work; timeline cap throws into execution; outbox sequence reuse can invalidate durable projector cursor. (P1) cancel has no replay/outbox terminal; queue lacks char-bounded text/reasoning coalescing and explicit producer backpressure; repository terminal writer bypasses timeline capacity. (P2) notification failures have warning logs but no metric. All require disposition before commit.
- Review repair design: reserve sequence 10,000 for repository terminal replay; streamed log persistence failure is counted in `agent_notification_metrics` and never changes execution. Queue coalesces text/reasoning only through 8,192 chars; `push=false` aborts the execution signal, throws to stream consumers, and awaits their stop. Cancel now writes timeline/outbox and notifies projector. Agent outbox v7 changes to AUTOINCREMENT/no FKs; deletion redacts to contiguous `run_deleted` tombstone. v8 adds redacted notification counters.
- Session recovery (2026-07-17): current working tree contains the Stage 2 repair WIP plus `plan.md` as the user-provided acceptance source. The recorded re-review had been requested but no live worker remains, so a new independent standards/spec review must run before Stage 2 can be marked verified or committed.
- Independent Stage 2 spec re-review findings (2026-07-17): `stream-consumers.ts` and final-output paths persist raw tool `output`, violating Stage 2 redact/size/artifact requirements; `deep-agent-executor.ts` still backfills final tool terminal events after the stream path; startup reconcile marks interrupted without checkpoint/effect evidence; terminal failure replay omits realtime `diagnostic` and `suggestion`. Repair all four with focused regressions before final verification.
- Independent Stage 2 standards re-review finding (2026-07-17): `reconcileStartupRuns()` selects background-task placeholder `running` rows with null executable snapshot/provider/model and calls `requireNonEmpty`, breaking plugin initialization after restart. Reconcile must exclude non-executable placeholder rows, with a normal background-task restart regression.
- Stage 2 re-review repair (2026-07-17): tool outputs now flow through one projector for main and subagent streams. It redacts before either inline projection or artifact persistence, caps inline output at 4,096 chars, and returns only a stored artifact preview for larger output. Final-output tool backfill was removed. Startup reconciliation excludes snapshotless background placeholders; snapshot-backed runs inspect checkpoint and in-flight/unknown effects before retaining only safe `waiting_user` state or writing an explicit interruption reason. Terminal failure transaction now preserves diagnostic/suggestion across task event, replay, and outbox. A full legacy timeline replaces sequence 10,000 with its terminal replay event.
- Full-suite Stage 2 regression follow-up (2026-07-17): `run_cancelled` had a durable replay event but renderer terminal detection/state application omitted it; fixed as the current cancel contract requires. Kernel schema fixture still expected agent v3 although the accepted migration ledger is v8; updated the stale assertion. Both focused regressions pass.
- Final review repair (2026-07-17): `failRun()` now passes diagnostic/suggestion into the single durable terminal writer; a regression compares the resulting replay event to the live event. Artifact projection errors now leave the native tool outcome untouched and propagate to the run boundary, preventing a completed effect from being reported to the model as `phase: 'error'`. `ChatRunState` includes `cancelled`. The requested automatic-resume branch is intentionally not added: restart never replays unfinished model/tool execution; snapshot/checkpoint/effect evidence only distinguishes safe waiting-user preservation from explicit interrupted reasons, and explicit resume remains the only continuation entry point.
- Release-spec repair (2026-07-17): a waiting queue consumer no longer receives text/reasoning deltas synchronously; delivery waits for a short window or queue close, so 10,000 fast deltas coalesce before persistence. `projectAgentOutbox()` returns an auditable applied-count/cursor result; plugin EventBus subscribers contain projection errors and `task.outbox.replay` is the explicit recovery action after a task-DB failure.
- Commit-gate standards repair (2026-07-17): queue capacity now compacts consumed prefix entries before it tests capacity. The cap and high-water metrics therefore represent only unconsumed work; a slow consumer cannot cause false overflow/abort solely because earlier entries remain in the backing array.
- Final standards repair (2026-07-17): `run_failed` outbox projection now mirrors the old task failure path by synchronizing agent-history thread status to `paused` and recording `background_task_paused`. Cursor idempotency prevents normal replay; an existing matching event check protects an interrupted cross-database projection retry from adding duplicate audit events.
- Stage 2 final gate (2026-07-17): optional `run_failed.diagnostic` and `suggestion` are now declared in `AgentOutboxEvent`, persisted by the terminal writer, and strictly reconstructed from the durable outbox. Focused regression passed. Independent standards and Stage 2 spec reviews both found no further issues. Fresh strict unused/typecheck, IPC, diff, build, and full Vitest (296 files / 1556 tests) passed; Vitest exit code was 0 despite the known post-run node-pty `AttachConsole failed` teardown output. Independently committed.
- Stage 3 product decision (2026-07-17): 用户以“继续”接受上轮推荐。Scheduler 采用同 task 不重叠；睡眠/关机造成的多次错过只合并执行最新 occurrence，必须保留可审计的 coalesce/skip reason，不补跑无限历史。
- Stage 3 baseline (2026-07-17): `TaskScheduler.fire()` 仍是 `startRun()` 后才插入随机 id 的 `scheduled_task_runs` 并更新 `background_tasks.run_id`；没有 occurrence key、task revision、claim 或 lease。`scheduled_task_runs` 只能按 task/time 查询，不能防 duplicate fire/restart；`background_tasks.run_id` 只保留最新 run，不能作为旧 occurrence 的权威关联。
- Stage 3 Step 1 green (2026-07-17): task schema v4 新增 `background_tasks.task_revision` 与 `scheduled_occurrences`（唯一 `(task, scheduledAt, revision)`、claim lease、attempt、dispatch key、run link、冻结 request、终态时间/原因）。`claimDueScheduledOccurrence()` 在同一 task DB transaction 内创建/claim occurrence 并推进下一次触发；已 claim snapshot 不受 task update 影响，task update 递增 revision。聚焦迁移/repository 测试通过。
- Stage 3 Step 2 green (2026-07-17): `ChatStartRunRequest`/capability schema 接受仅 `background_schedule` 可用的 `dispatchKey`；runtime 把它冻结进 snapshot，并在创建前及 thread-lease/unique-key竞争后查询既有 run。重复同 key 返回同一 `ChatStartRunResult`，不重新排队执行；snapshot rehydration 保留该 key。
- Stage 3 Step 3a green (2026-07-17): scheduler 已由 `startRun -> random scheduled_task_run` 改为 `claimDueScheduledOccurrence -> startRun(dispatchKey) -> markScheduledOccurrenceDispatched`。claim 在 agent 启动前持久化；agent 启动失败只将已 claim occurrence 落为 `failed` 并暂停 task。若 agent 已启动而 task DB link 失败，occurrence 保持可 reconcile 的 claim，不伪造启动失败。
- Stage 3 Step 3b/4 green (2026-07-17): cron misfire 只 materialize 最晚一次并写 `misfire_coalesced_latest`；前 run 未终态时新 occurrence 仅写 `pending/overlap_coalesced_latest`。过期 claim 在 restart reconcile 释放；dispatched occurrence 从 agent DB 终态收敛，`interrupted`/missing 只标 `unknown`。outbox projector 用 occurrence `run_id` 而非 `background_tasks.run_id` 找回终态，旧 run 不会覆盖或暂停较新的 task 摘要。
- Stage 3 power-resume green (2026-07-17): `handlePowerResume()` 重注册 timer 后，cron 从多个错过时间只 dispatch 最新 occurrence；审计保留 `misfire_coalesced_latest`，`next_run_at` 前进到下一未来时间。测试只推进当前 due timer，不能用 `runAllTimersAsync` 穿透无限 cron。
- Stage 4 Part 1 continuation review (2026-07-19): frozen manifest 现为 tool authorization 单一事实源。Compiler 冻结 runtime/background inventory；executor 仅从 manifest 选择 background/MCP tools；builder 在 main prompt 组装前过滤 subagent tool surface；protocol 在 handler 前拒绝 identity drift、missing membership 与 scope mismatch。Fresh 8-file focused gate、typecheck、strict unused、IPC、diff check 均通过；未发现新的 Part 1 finding。
- Stage 4 Part 1 final gate (2026-07-19): CodeGraph final call-path review 与 preview API/schema 核对未发现缺口；`agent.capability.preview` 是普通能力面 API，工作流 run preview 由 `startRun` 内部传入 frozen `workflowHint`。Fresh build 成功；全量 Vitest 298 files / 1583 tests passed，exit 0；仅有既知 Windows `node-pty AttachConsole failed` teardown 噪音。
- Stage 4 Part 2 initial boundary (2026-07-19): `plan.md` 要求 native model/tool call limits 覆盖 main/subagent，Roc 只负责 immutable per-run budget policy、wall/deadline、usage/cost 与结构化终止；当前 discovery 尚未确认现有 middleware 是否已装配原生 limits。
- Stage 4 Part 2 focused implementation (2026-07-19): LangChain 1.5.3 native middleware uses persisted state counters and throws named model/tool limit errors when `exitBehavior: 'error'`; Roc maps both to `run_budget_exhausted` with `retryable: false`, so `toRecoveryDecision()` cannot retry budget exhaustion. New run writes snapshot V2; persisted V1 snapshots remain readable through deterministic origin/mode policy migration.
- Stage 4 Part 2 review repair (2026-07-19): initial implementation passed only `runLimit`; spec requires both native run/thread limits. V2 budget now freezes model/tool run and thread limits, with background values lower than interactive; builder requires all four values. Runtime terminal regression confirms one executor attempt, failed run, stable Chinese budget message, and no `run_recovering` event.
- Stage 4 Part 2 final gate (2026-07-19): no further review findings after thread-limit repair. `check:ipc`, build, and full Vitest 298 files / 1587 tests passed; native budget exhaustion is structurally non-retryable and persists V2 snapshot limits.
- Stage 4 Part 3 initial boundary (2026-07-19): product gate remains interactive shell without approval; background shell requires durable pre-authorization at task creation; host execution is not workspace isolation. Discovery pending for real service adapter and existing task authorization persistence.
- Stage 4 Part 3 implementation/review (2026-07-19): `allowedActions` is persisted into scheduled occurrence request and copied into the run snapshot; empty background authorization omits `run_shell_command` from the manifest and executor surface. Agent shell requests carry run/thread/AbortSignal; service enforces exact normalized command authorization, minimal environment, fixed deadline, output cap, artifact preview, and Windows `/T /F` tree termination. Renderer IPC strips internal authorization/signal fields. No new review finding after focused, strict, IPC, scheduler, build, path, cancellation, and full-suite verification.
- Stage 4 Part 3 final boundary repair (2026-07-19): generic `RTKMiddleware` no longer rewrites `run_shell_command` before the Roc shell service; `CommandRewriter` now accepts the run signal and service allowlisted environment. This keeps authorization, rewrite, child process, cancellation, and output controls on one execution path.
- Stage 4 Part 4 discovery (2026-07-19): `WebReadService.requirePublicUrl()` only validates protocol, while the actual outbound path always discloses the target URL to `https://r.jina.ai/`; it returns body text without source/hash/trust provenance and reads the full body before any local cap. The tool function can consume LangChain `RunnableConfig.signal`, so no Roc-specific cancellation API is needed at the model surface.
- Hook discovery: `HookRuntime.runEvent()` starts every selected handler through `Promise.all`; a blocking first handler cannot prevent later side effects. `HookCommandRunner` has timeout tree termination but no run signal. The correct boundary is ordered execution with per-result merge/short-circuit, and the same graph signal passed through hook middleware/runtime into the runner.
- Stage 4 Part 4 review repair (2026-07-19): `SessionEnd` was a separate lifecycle path and initially lacked the run signal; cancel could abort the executor but still start a new terminal hook. The lifecycle emitter now requires the signal, cancel aborts before emission, and already-aborted HookRuntime/runner paths fail closed without spawning.
- Stage 4 Part 4 verification (2026-07-19): full Vitest first exposed a pre-existing shell cancellation PID-file read race (`EBUSY`) under parallel tests; isolated rerun passed. The fixture now waits for an unlocked valid PID and always settles/aborts before cleanup; fresh full suite is 299 files / 1604 tests passed.

## Verification Rules

- TypeScript 改动至少运行 focused test、`pnpm typecheck`、`git diff --check`。
- 数据库、路径、打包、IPC 改动按 `plan.md` 的专项矩阵扩展。
- 最终清理运行 strict unused scan、typecheck、IPC check、build、test、diff check。

## Review Results

- Stage 0 Step 1 standards review：无问题。
- Stage 0 Step 1 spec review：无问题；deferred barrier、fake timers 和 runId/status 断言覆盖实际并发路径。
- Stage 2 re-review findings (2026-07-17): (1) `run_cancelled` reached persistence/projector but renderer terminal detection/reducer omitted it, leaving replay and live UI in `running`; (2) `Promise.all` rejected before all Deep Agent producers settled after queue overflow; (3) a pre-cap non-terminal run with 10,000 replay rows could make startup reconciliation fail while allocating its terminal event. Repairs require one renderer terminal-state branch, producer `allSettled` cleanup, and transactionally reserving sequence 10,000 by truncating only overflowed non-terminal stream tail before terminal replay.
