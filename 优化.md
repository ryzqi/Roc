# Roc 优化计划

## 当前状态

本次审计只修改 Markdown 追踪文件，未改产品源码。当前主要问题不是“到处都是无用代码”，而是：

- 顶层编排层过厚：`App.tsx`、`DeepAgentRuntimeService`、`registerIpc`、`GitWorkbench`、`SettingsView` 承担过多横向职责。
- 局部重复实现明确存在：错误分类、文本校验、IPC unwrap、StatusPill、文件树/skill 树浏览、renderer test loaded-state helper。
- CSS 单文件过大：`main.css` 5023 行，跨 chat/git/provider/settings/workbench/composer/terminal/memory/skills/floating。
- 测试代码自身也过厚：`electron-smoke.mjs` 3129 行，`app-services.test.ts` 2574 行，`deep-agent-runtime-service.test.ts` 2338 行。
- 图谱 dead-code 候选不能直接删除，里面包含动态入口、React handler、Electron builder hooks、第三方接口方法。

## 优化原则

1. 先收口明确重复，再拆大文件，最后考虑删除候选。
2. 每一步只移动或抽取一个责任边界，不同时重构行为和视觉。
3. 保留现有 `data-testid`、IPC channel、provider/runtime contract、Deep Agents `/skills/` 和 `/memory/` contract。
4. 不引入 DI 容器、插件系统、通用框架或兼容层来解决一次性重复。
5. 删除代码前必须有二次核验：静态引用、动态入口、测试覆盖、打包配置都通过后再删。

## 阶段 1：低风险重复 helper 收口

目标：先降低重复，不改变架构形状。

### 1.1 统一 renderer IPC unwrap

- 现状：`src/renderer/loaded-state.ts`、`src/renderer/settings/index.tsx`、`tests/smoke/electron-smoke.mjs` 都定义 `unwrap()`。
- 操作：让 renderer 组件统一导入 `loaded-state.ts` 的 `unwrap()`；smoke helper 保留或抽到 `tests/smoke/lib/ipc.mjs`，不要让产品代码依赖测试 helper。
- 验收：

```powershell
pnpm vitest run tests/renderer/settings-floating-surfaces.test.ts tests/renderer/providers-section.test.ts
pnpm typecheck
```

### 1.2 统一 StatusPill

- 现状：`components/StatusPill.tsx`、`settings/atoms.tsx`、`skills-view.tsx` 各自实现状态徽标。
- 操作：优先保留 `components/StatusPill.tsx` 作为共享组件；settings/skills 只保留必要 wrapper 或直接复用。
- 注意：先核对 className 和视觉差异，不要顺手改样式。
- 验收：

```powershell
pnpm vitest run tests/renderer/skills-view.test.ts tests/renderer/providers-section.test.ts tests/renderer/settings-floating-surfaces.test.ts
```

### 1.3 统一 renderer test loaded-state factory

- 现状：`chat-composer.test.ts`、`chat-view.test.ts`、`view-test-helpers.ts` 都有 `createLoadedState`。
- 操作：统一到 `tests/renderer/view-test-helpers.ts`，删掉局部重复 fixture。
- 验收：

```powershell
pnpm vitest run tests/renderer/chat-composer.test.ts tests/renderer/chat-view.test.ts tests/renderer/workbench-surfaces.test.ts
```

### 1.4 收口文本校验 helper

- 现状：memory/task/config/mcp/skill/diagnostics 都存在 trim + domain error 逻辑。
- 操作：在 `src/main/services` 内建立小型 `validation` helper，仅封装“非空文本 + RocDomainError 参数”，各 domain 继续提供自己的 code/message/userAction。
- 禁止：不要把不同 domain 的错误码合并成通用错误。
- 验收：

```powershell
pnpm vitest run tests/main/app-services.test.ts tests/main/config-service.test.ts tests/main/services/skill-service.test.ts
```

## 阶段 2：Renderer 顶层编排拆分

目标：降低 `App.tsx`、`ChatComposer`、`GitWorkbench` 的变更半径。

### 2.1 拆 `ChatComposer` popover

- 现状：tools、skills、models 三个 popover 在 `chat-composer.tsx` 内联，tools/skills 结构高度同构。
- 操作：先抽 `CapabilityPopover`，只接收 title/count/copy/items/selected/selectAll/clearAll/renderItem。
- 保留：`chat-tool-trigger`、`chat-skill-trigger`、`chat-model-trigger`、`chat-task-submit` 等稳定 selectors。
- 验收：

```powershell
pnpm vitest run tests/renderer/chat-composer.test.ts
pnpm typecheck
```

### 2.2 拆 `App.tsx` lazy load effect

- 现状：workspace/memory/operations 三段 effect 结构相同，只有 target/key/loader/error copy 不同。
- 操作：抽 `useLazyStartupResource()` 或等价小 hook，输入 load intent、key、loader、applyState、setLoadState。
- 禁止：不要改启动加载策略和 `getStartupLoadIntent()` 的业务语义。
- 验收：

```powershell
pnpm vitest run tests/main/startup-load-policy.test.ts tests/renderer/workspace-surfaces.test.ts tests/renderer/memory-view.test.ts
```

### 2.3 拆 `WindowWorkband`

- 现状：`App.tsx` 同时处理 window drag、titlebar buttons、chat history actions、top meta。
- 操作：抽 `WindowWorkband` 组件和 `useWindowDrag()` hook；保留 window IPC 调用方式与按钮 selector。
- 验收：

```powershell
pnpm vitest run tests/renderer/workspace-surfaces.test.ts tests/renderer/chat-view.test.ts
```

### 2.4 拆 `GitWorkbench`

- 现状：`GitWorkbench` 668 行，内部包含 diff request、branch switcher、commit/stage command、split resize 和 JSX。
- 操作顺序：
- 先抽 `useGitDiffPreview()`，封装 request id、loading/failed path、cache 和 `window.roc.git.fileDiff()`。
- 再抽 `GitBranchPopover`，保留 branch create/checkout confirm 行为。
- 最后抽 `GitDiffPanel`，只负责 selected diff render。
- 验收：

```powershell
pnpm vitest run tests/renderer/git-workbench.test.ts tests/renderer/workbench-surfaces.test.ts
```

## 阶段 3：Main/runtime 职责拆分

目标：降低 Deep Agents runtime 与 config/provider 链路的维护风险。

### 3.1 抽 Deep Agent session assembly

- 现状：`executeRun()` 与 `executeResume()` 都构建 tools、backend、skillSources、agent、configurable、stream consumers。
- 操作：抽 `createDeepAgentSession(context)`，返回 agent/run tools/closers/configurable；start/resume 只提供不同输入。
- 保留：`memorySources: ['/agents/AGENTS.md']`、`skillSources: /skills/{id}/`、checkpointer 条件、interruptOn 条件。
- 验收：

```powershell
pnpm vitest run tests/main/deep-agent-runtime-service.test.ts tests/main/deep-agent-backend.test.ts tests/main/deep-agent-tools.test.ts
```

### 3.2 抽 stream consumer

- 现状：`consumeMessages()`、`consumeToolCalls()`、`consumeSubagents()` 同时做 record parsing、task event persistence、runtime event emit。
- 操作：抽 `deep-agent/stream-consumers.ts`，传入 small callbacks：`emitRuntimeEvent`、`recordTaskEvent`、`markVisibleOutput`。
- 禁止：不要改变 visible text boundary；`SKILL.md`、hosted MCP/tool raw payload 仍不能进入 assistant 可见文本。
- 验收：

```powershell
pnpm vitest run tests/main/deep-agent-runtime-service.test.ts tests/main/record-utils.test.ts tests/main/deep-agent-prompt.test.ts
```

### 3.3 合并 start/resume completion/failure 逻辑

- 现状：`executeRun()` 和 `executeResume()` 重复 provider result build、usage log、task complete/fail、run_completed/run_failed emit。
- 操作：抽 `completeRun(context, usage, assistantChunks)` 与 `failRun(context, error)`；resume 额外删除 pending interrupt。
- 验收：

```powershell
pnpm vitest run tests/main/deep-agent-runtime-service.test.ts
```

### 3.4 收口 provider error classifier

- 现状：`provider-request-retry.ts` 与 `deep-agent/error-mapping.ts` 重复 timeout/network/abort/status 判断。
- 操作：在 provider 层暴露纯分类函数，error-mapping 只负责映射成 run failure 文案。
- 验收：

```powershell
pnpm vitest run tests/main/provider-request-retry.test.ts tests/main/deep-agent-runtime-service.test.ts tests/main/app-services.test.ts
```

## 阶段 4：Config / Provider / Settings 数据流收口

目标：减少 fixed provider 新增或变更时的多点修改。

### 4.1 拆 `ConfigService`

- 现状：schema/defaults/migration/provider mutation/default model state/settings IO 同文件。
- 操作顺序：
- `config/schema.ts`：Zod schema 和类型。
- `config/defaults.ts`：default settings/providers/mcp/permissions/shortcuts。
- `config/migration.ts`：legacy split/v2/v4 migration。
- `config/provider-rules.ts`：fixed provider normalize、delete reset、default model sanitization。
- 保留 `ConfigService` 作为 IO 和 orchestration 门面。
- 验收：

```powershell
pnpm vitest run tests/main/config-service.test.ts tests/main/fixed-nvidia-provider-config.test.ts tests/main/app-services.test.ts
```

### 4.2 拆 `settings-model.ts`

- 现状：provider draft、model parsing、save request、impact rows/copy 同文件。
- 操作顺序：
- `settings/provider-draft-model.ts`
- `settings/settings-save-model.ts`
- `settings/impact-model.ts`
- 保留原导出兼容一轮，或同步更新全部 import 后删除旧聚合。
- 验收：

```powershell
pnpm vitest run tests/renderer/settings-model.test.ts tests/renderer/providers-section.test.ts
pnpm typecheck
```

### 4.3 明确 fixed provider 事实源

- 现状：`provider-defaults.ts` 已是 defaults 事实源，但 main/renderer/runtime 仍分散处理 fixed provider 行为。
- 操作：列出 provider 新增时必须改的唯一清单；能下沉到 `provider-defaults.ts` 的只下沉纯数据和 normalize，不下沉 UI 文案。
- 验收：

```powershell
pnpm vitest run tests/main/langchain-model-factory.test.ts tests/main/config-service.test.ts tests/renderer/settings-model.test.ts
```

## 阶段 5：CSS 分域切分

目标：降低 `main.css` 5023 行全局耦合，但不改变视觉。

### 5.1 先建立 import 边界

- 操作：把 `main.css` 按域移动到：
- `styles/app-shell.css`
- `styles/chat.css`
- `styles/composer.css`
- `styles/settings.css`
- `styles/workbench.css`
- `styles/git.css`
- `styles/terminal.css`
- `styles/memory.css`
- `styles/skills.css`
- `styles/floating.css`
- 保留 `main.css` 作为 import 汇总入口。
- 禁止：第一步只移动规则，不改 selector、变量、顺序依赖和视觉值。
- 验收：

```powershell
pnpm build
pnpm vitest run tests/renderer/chat-composer.test.ts tests/renderer/workbench-surfaces.test.ts tests/renderer/settings-floating-surfaces.test.ts
```

### 5.2 再清理重复选择器

- 操作：只在分域后处理重复 `.status-pill`、`.tool-row`、`.composer-*`、`.git-*` 规则。
- 验收：需要在 UI smoke 或截图稳定后执行。

```powershell
pnpm smoke:electron
```

## 阶段 6：测试与 smoke 结构化

目标：测试仍覆盖同样行为，但单文件更易维护。

### 6.1 拆 smoke helper

- 现状：`tests/smoke/electron-smoke.mjs` 同时处理 launch、data seed、settings、chat、workbench、assert、artifact、cleanup。
- 操作：先抽纯 helper：
- `tests/smoke/lib/ipc.mjs`
- `tests/smoke/lib/fixtures.mjs`
- `tests/smoke/lib/ui-actions.mjs`
- `tests/smoke/lib/assertions.mjs`
- `tests/smoke/lib/artifacts.mjs`
- 保留入口 `electron-smoke.mjs` 串联主流程。
- 验收：

```powershell
pnpm smoke:electron
```

### 6.2 拆 main 集成测试

- 现状：`app-services.test.ts` 覆盖 config/memory/files/git/shell/provider/task 多域。
- 操作：按域拆为 `app-services.memory.test.ts`、`app-services.git.test.ts`、`app-services.provider.test.ts`、`app-services.tasks.test.ts`，共享 fixture 放 `tests/main/app-service-fixtures.ts`。
- 验收：

```powershell
pnpm vitest run tests/main/app-services*.test.ts
```

### 6.3 给顶层编排补小测试

- 目标：补 `App`、`SettingsView`、`GitWorkbench`、`registerIpc` 的直接约束，减少只靠 smoke 的反馈延迟。
- 操作：优先测试可观察行为：导航、settings open/close、lazy load trigger、IPC handler register count/route mapping。
- 验收：

```powershell
pnpm vitest run tests/renderer/app*.test.ts tests/renderer/settings*.test.ts tests/main/*ipc*.test.ts
```

## 阶段 7：Dead-code 候选二次核验

目标：只删除被证明没有动态入口的代码。

### 核验清单

- Builder hooks：保留，已由 `electron-builder.yml` 引用。
- React handler：需要 JSX 引用核验，默认保留。
- Third-party interface methods：`ReadOnlyStoreBackend`、`SqliteLangGraphStore.batch()` 等先保留，除非能证明 Deep Agents/LangGraph 不调用。
- Helper 函数：`viewMeta`、`formatBytes`、`selectedGitPaths`、`gitSelectableChanges` 这类可用 `rg` 全仓确认后删除。

### 验收

```powershell
rg "viewMeta|formatBytes|selectedGitPaths|gitSelectableChanges" src tests scripts
pnpm typecheck
pnpm test
```

## 推荐执行顺序

1. 阶段 1.1、1.2、1.3：低风险 helper 收口。
2. 阶段 2.1：`ChatComposer` popover 抽取。
3. 阶段 2.2、2.3：`App.tsx` lazy load 和 workband 拆分。
4. 阶段 3.1、3.2、3.3：Deep Agents runtime 拆分。
5. 阶段 4：Config/provider/settings 数据流收口。
6. 阶段 5：CSS 只移动后清理。
7. 阶段 6：测试结构化。
8. 阶段 7：dead-code 二次核验后删除。

## 不建议做的事

- 不要第一步删除 code-review-graph 标记的 dead-code。
- 不要为了服务装配列表重复而引入 DI 容器。
- 不要把 settings/provider 的 UI 文案、runtime default、schema validation 全塞进一个“万能 provider registry”。
- 不要在 CSS 切分时同时改视觉。
- 不要在 Deep Agents runtime 拆分时改 `/skills/`、`/memory/`、visible text filtering 或 HITL 行为。
- 不要用 smoke 脚本重构替代产品代码测试；两者要分开做。

## 完整验证矩阵

小步验证：

```powershell
pnpm typecheck
pnpm vitest run tests/renderer/chat-composer.test.ts
pnpm vitest run tests/renderer/settings-model.test.ts tests/renderer/providers-section.test.ts
pnpm vitest run tests/main/provider-request-retry.test.ts tests/main/config-service.test.ts
pnpm vitest run tests/main/deep-agent-runtime-service.test.ts
```

阶段性验证：

```powershell
pnpm test
pnpm build
```

交付验证：

```powershell
pnpm package:dir
pnpm smoke:electron
```

## 成功标准

- `App.tsx` 不再同时承载 lazy loading、window workband、history menu、workbench orchestration 和 shell JSX。
- `DeepAgentRuntimeService` 保留 run lifecycle 门面，agent assembly、stream consumers、completion/failure 被拆到可测试小模块。
- `main.css` 变成 import 汇总，不再作为跨所有页面的单一编辑热点。
- `tests/smoke/electron-smoke.mjs` 保留入口流程，但 helper/fixture/assertion 分离。
- 重复 helper 数量下降，新增 provider、settings section、workbench 行为时不再改 4 到 6 个不相邻文件。
- 所有删除都有直接引用核验和测试证据支撑。

