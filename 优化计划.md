# Roc Windows 桌面 native-feel 优化计划

## 当前状态

状态：Changed, unverified。本文只讨论 Windows 上的 Roc.exe，范围限于当前 Windows Electron 产物，不规划宿主重写。依据来自当前仓库代码、`ace-tool` 检索、`code-review-graph` 结果和本地 Electron 类型定义；本文档本身尚未执行运行时验证。

目标：在现有 Electron + React 架构内，把 Roc 做得更像 Windows 桌面应用：窗口行为正确、托盘真实、快捷键可用、主题跟随系统、输入与滚动少网页感、性能指标能解释用户感知。

边界：

- 继续以当前 Electron 宿主为基础优化 Windows 体验。
- 不重写 UI 技术栈。
- 不引入新的宿主技术路线。
- 不把视觉重绘排在系统行为前面。
- 不把 Electron/Chromium baseline 当成必须消灭的性能问题；只优化 Roc 自己产生的 margin cost。

## Windows-only 判定

当前项目已经是 Windows-first：

- `package.json`：`name` 是 `roc-windows-super-assistant`，描述是 `Roc Windows local desktop assistant`。
- `electron-builder.yml`：只配置 `win` target，产物是 `release\win-unpacked\Roc.exe`。
- `scripts\package-dir.mjs` 和 `scripts\lib\native-packaging.mjs` 已围绕 Windows packaged exe、`better-sqlite3` Electron ABI、运行中 `Roc.exe` 清理做了专门处理。
- smoke gate 已支持 dist 与 packaged exe 两种目标，Windows packaged smoke 是当前最可信验收路径。

native-feel 技能的决策树结论：

- Roc 当前只规划 Windows 产物；四层 native shell + WebView2 + Node + Rust 架构的跨平台收益不成立。
- 继续用 Electron 的代价是无法达到“与 C#/WinUI/WPF 宿主不可区分”的上限，尤其是窗口 material、系统托盘、热键、登录项、系统通知和 crash/update 集成。
- 当前计划因此不是宿主重写计划，而是 Windows Electron ceiling 内的 native-feel remediation。若验收目标改成“不可区分于原生 Windows 应用”，应另开 C#/WinUI 或 WPF 宿主迁移计划。

适用原则：

- T1：把平台能力放在渲染面以下。Roc 现阶段不能重画系统托盘、热键、登录项、窗口 material，应在 Electron main process 调 Windows/Electron 宿主 API。
- T3：采用平台，不和平台竞争。能用 `Tray`、`globalShortcut`、`dialog.showMessageBox()`、`nativeTheme`、`setLoginItemSettings()` 的地方，不用 DOM 或 CSS 模拟。
- T4/T8：性能看用户感知，并区分 Electron/Chromium baseline 与 Roc margin cost。先测 ready-to-show、hotkey 到 quick entry、隐藏/驻留 CPU、IPC top N，再谈优化。
- T6：跨进程边界必须有频率和负载证据。`window:set-bounds` 这类拖动高频 IPC 是优先清理对象。

结论：优化计划聚焦现有 Windows Electron 宿主，不做平台迁移路线图，但会明确 Electron 路线的 native-feel 上限。

## 证据摘要

| 主题 | 当前证据 | 影响 |
|---|---|---|
| Windows 打包 | `electron-builder.yml` 只有 `win` target；`package:dir` 产出 `release\win-unpacked\Roc.exe` | 验收必须覆盖真实 packaged exe |
| 主窗口 | `src/main/window-shell.ts` 使用 `frame: false`、固定 `width/height/minWidth/minHeight`、静态 `backgroundColor`、`sandbox: false` | 需要补 Windows 窗口行为、material、安全边界 |
| 启动展示 | `src/main/index.ts` 使用 `ready-to-show` 后 `show()`，并延后 `initializeDeferredServices()` | 首帧控制已有基础，可继续收紧启动指标 |
| 浮动入口 | `openFloatingEntry('quick'|'tray')` 创建 quick/tray BrowserWindow | quick 可以做热入口；tray entry 不是系统托盘 |
| 系统托盘 | `LifecycleService.getTraySummary()` 只有状态；没有 Electron `Tray` 命中 | `minimizeToTray` 语义未真正落到 Windows tray |
| 全局快捷键 | settings 有 `globalHotkey`；设置页提示“仅保存键位字符串，未注册系统级快捷键” | 用户配置不产生系统效果 |
| 开机启动 | settings 有 `startup.openAtLogin`；主进程未见 `setLoginItemSettings` 命中 | 设置存在但 Windows 登录项未落地 |
| 单实例 | `requestSingleInstanceLock` 未在 `src` 命中 | 二次启动可能产生第二实例 |
| 窗口拖动 | `WindowWorkband` 指针事件调用 `use-window-drag.ts`；拖动时通过 `window.roc.window.setBounds()` 高频 IPC | 和 Electron native drag 区重复，拖动感知风险高 |
| Window controls | 自绘 `−`、`↗`、`×` glyph | 需要匹配 Windows caption hit target、hover、pressed、close red |
| WebView 行为 | `base.css` 全局 `button { cursor: pointer; }`；`chat-transcript-panel.tsx` 使用 smooth scroll | 网页感明显 |
| Confirm/dialog | workspace/file 选择已用 `dialog.showOpenDialog()`；Git 危险操作仍用 `window.confirm()`；Settings/TaskCreate 是 DOM dialog | native dialog 使用不一致 |
| 视觉 token | `tokens.css` 硬编码 accent、gradient、Inter first、popover/modal shadow | 未跟随 Windows accent、Mica、系统字体优先级 |
| IPC | `preload/index.ts` 手写 `window.roc`；`shared/ipc.ts` 有 TS 类型；`register-ipc.ts` 集中注册大量 handler | 类型基础可用，但中心文件和 IPC 出度过高 |
| 性能采样 | `DiagnosticsService.samplePerformance()` 只看 main process `process.memoryUsage()` | 不能解释 renderer/GPU/utility 成本 |
| Renderer entry | `electron.vite.config.ts` 已有 `index`、`quick`、`tray` 三入口 | 已有按窗口拆 bundle 的基础，可继续用 hotkey latency 决定是否预热 quick |
| 外部链接 | 主窗口和浮窗都 `setWindowOpenHandler` deny 后直接 `shell.openExternal(url)` | 需要 URL scheme allowlist，避免任意 scheme 外跳 |
| 内部协议 | `roc-preview://workspace/pdf/...` 只服务 PDF preview | 不是系统 URL scheme；若要 deeplink 需新增 app protocol 注册和 single-instance 参数分发 |
| 通知 | settings 有 `notifications.lowDistraction`；主进程未见 Windows toast/Notification 落地 | 常驻后台任务缺系统通知路径 |
| 任务栏 | 未见 `setAppUserModelId`、`setUserTasks`、`setJumpList`、taskbar progress/overlay 命中 | Windows shell identity 和后台任务状态尚未形成系统入口 |
| Updater/signing | `electron-builder.yml` 只有 `dir` target，`signAndEditExecutable: false` | 尚无 installer、签名、auto-update 计划，不能按发布级 Windows app 验收 |
| Crash/logging | 有 `LogService` 与 diagnostic package；未见 crash reporter | 运行时崩溃只能靠日志/diagnostic，缺 symbolicated crash 证据 |
| Clipboard | code/task 复制使用 `navigator.clipboard.writeText()` | 只覆盖纯文本，缺 rich clipboard / native clipboard 策略 |
| Accessibility | DOM 有部分 ARIA；没有 Narrator/focus order/large font smoke | native-feel 不能只看视觉，需要可访问性验收 |
| 验收 | `pnpm smoke:electron`、`pnpm smoke:performance`、`pnpm package:dir` 已存在 | 应作为 Windows native-feel 变更主门槛 |

## 优先级

P0：先补 Windows native-feel 基线与 packaged smoke 证据。

P1：修 Windows 宿主生命周期：单实例、真实托盘、关闭/驻留、开机启动、全局快捷键、AppUserModelID。

P2：修窗口 chrome：native drag、caption hit target、Mica/acrylic、窗口位置恢复。

P3：修 WebView 行为：cursor、selection、scroll、context menu、confirm、keyboard、IME。

P4：修 Windows 主题与视觉 token：系统字体、system accent、dark/light、外层圆角/阴影边界。

P5：修 IPC、安全和性能测量：拆注册、IPC top N、跨进程 metrics、bundle/window 策略。

P6：补 Windows 发布级系统集成：URL scheme、file association、toast、taskbar/Jump List、native clipboard、native file dialogs、drag-and-drop、signing/installer/updater、crash reporter、Narrator。

## Phase 0：Windows native-feel 基线

目标：先让“像 Windows 应用”变成可测结果。

当前已具备的基线：

- `tests/smoke/performance-smoke.mjs` 已记录 `smokeTarget.kind/path`、`packagedExeExists`、`quickOpenMs`、`trayOpenMs`，并在前后各采一次 `samplePerformance()`。
- `tests/smoke/electron-smoke.mjs` 已记录 `smokeTarget.kind/path`，并直接调用 `window.roc.diagnostics.samplePerformance()` 与 `window.roc.lifecycle.getTraySummary()`。
- `src/main/index.ts` 已绑定 `powerMonitor.on('resume')` 到 `taskSchedulerService.handlePowerResume()`；这一项不是从零新增，而是需要验证只注册一次且 packaged 行为一致。

动作：

1. 新增 Windows native-feel scorecard，覆盖：
   - cold launch / ready-to-show
   - packaged exe 启动
   - close/minimize/tray 行为
   - global hotkey
   - single instance
   - Mica/acrylic fallback
   - cursor/selection/scroll
   - native context menu/dialog
   - keyboard/IME
   - process metrics
   - system notification / URL scheme / file association
   - taskbar identity / Jump List / task progress
   - signing / installer / updater / crash reporter
   - Narrator / large font / high contrast / forced colors
2. 扩展现有 `tests/smoke/performance-smoke.mjs` 输出：
   - 保留当前 `smokeTarget.kind/path`、`packagedExeExists`、`quickOpenMs`、`trayOpenMs`
   - main ready-to-show
   - renderer ready
   - BrowserWindow 数量
   - Electron process metrics
3. 收紧 soft warning：
   - main cold ready-to-show > 800ms：warning
   - quick entry open > 500ms：warning
   - warm quick reopen > 200ms：warning
   - renderer ready 后 500ms 内主输入不可用：fail
4. 新增静态 CSS 测试：
   - 禁止全局 `button { cursor: pointer; }`
   - 禁止默认 smooth scroll
   - 外层 app shell 不模拟 window shadow
5. 补齐 packaged smoke 缺失字段：
   - native module probe result
   - process metrics summary
   - `ready-to-show` / renderer-ready 摘要
   - BrowserWindow / process count 摘要

取舍：测试更严格，短期会暴露更多 warning。收益是后续优化不会靠主观判断。

验证：

```powershell
pnpm typecheck
pnpm build
pnpm smoke:performance
pnpm smoke:electron
```

完成标准：

- smoke artifact 可解释 Windows 启动、窗口、入口、性能状态。
- 引入 cursor/smooth scroll 回归时测试失败。

## Phase 1：Windows 宿主生命周期

目标：补齐 Windows 桌面应用最基础的系统行为。

动作：

1. 单实例：
   - 在主进程最早阶段调用 `app.requestSingleInstanceLock()`。
   - 第二实例退出。
   - `second-instance` 聚焦现有主窗口，恢复 minimized 状态。
   - 若 second instance 带路径或协议参数，先记录日志，不吞掉。
2. AppUserModelID：
   - 启动时调用 `app.setAppUserModelId('com.roc.desktop')` 或与 builder appId 对齐的稳定 ID。
   - 为 Windows 通知、任务栏归组、jump list 留出正确身份。
3. 真实系统托盘：
   - 新增 Electron `Tray` 管理模块。
   - 托盘菜单包含：打开 Roc、快速入口、暂停/恢复后台执行、打开任务、退出。
   - `LifecycleService.getTraySummary()` 继续做状态源。
   - `TrayEntryView` 改成托盘弹出详情入口，而不是“托盘本体”。
4. close/minimize 语义：
   - `startup.minimizeToTray=true`：主窗口 close 隐藏并驻留托盘。
   - `startup.minimizeToTray=false`：主窗口 close 退出。
   - explicit quit 必须真正退出，不留下后台 scheduler 或子进程。
5. 开机启动：
   - 保存 settings 时同步 `app.setLoginItemSettings()`。
   - settings 页面显示“配置值”和“系统实际状态”。
   - 同步失败给出可见错误。
6. 全局快捷键：
   - 读取 `settings.globalHotkey`。
   - 用 `globalShortcut.register()` 注册 quick entry。
   - 设置变化后重注册。
   - 注册失败写日志并在 settings impact/状态行中展示。
   - 退出时 unregister。
7. power resume：
   - 复查现有 `powerMonitor.on('resume')` 绑定只注册一次。
   - 防止重复 createWindow 后重复副作用。
   - packaged smoke 补一条 resume 后调度器仍可恢复的证据。

取舍：主进程状态机会变复杂。收益是 Roc 的“常驻助手”定位真正成立。

验证：

```powershell
pnpm vitest run tests/main/config-service.test.ts
pnpm vitest run tests/main/workspace-dialog-ipc.test.ts
pnpm build
pnpm smoke:electron
```

packaged 验收：

```powershell
pnpm package:dir
$env:ROC_SMOKE_TARGET = 'packaged'
pnpm smoke:electron
Remove-Item Env:\ROC_SMOKE_TARGET
```

完成标准：

- 二次启动只聚焦现有窗口。
- `minimizeToTray` 两种分支都被 smoke 或主进程测试覆盖。
- 设置全局快捷键后能打开 quick entry。
- 开机启动状态可读、可写、可报错。
- 托盘菜单能打开主窗口、暂停/恢复后台执行、退出。

## Phase 2：Windows 窗口 chrome 与 material

目标：让 frameless window 不像网页模拟窗口。

动作：

1. Native drag：
   - 用 `-webkit-app-region: drag/no-drag` 做主要拖动路径。
   - 删除或降级 `use-window-drag.ts` 的 pointer move `window.setBounds()` 高频 IPC。
   - 保留 smoke：拖动窗口后 bounds 确实变化。
2. Caption buttons：
   - 统一最小化、最大化/还原、关闭按钮尺寸和 hit target。
   - hover/pressed 状态匹配 Windows 11：关闭按钮 hover 使用 red background。
   - glyph 不使用容易误读的箭头，改为 Windows caption 语义图标或稳定 glyph。
3. Mica/acrylic：
   - 本地 Electron 类型定义支持 `BrowserWindow.setBackgroundMaterial('auto' | 'none' | 'mica' | 'acrylic' | 'tabbed')`。
   - 主窗口优先 `mica`。
   - quick/tray floating window 可评估 `acrylic`。
   - 不支持时 fallback 到静态背景。
4. 外层背景：
   - `BrowserWindow` 与 app shell 建立透明/material 链路。
   - body/app shell 不再铺满 gradient。
   - 内容 panel 保留 surface。
5. 窗口尺寸与位置：
   - 新增 app-state 文件保存 main window bounds、maximized。
   - 启动时用 `screen` 校验窗口在可见 display 内。
   - display 变化、DPI/scaleFactor 变化时重新校验 bounds。
   - quick entry 依据当前鼠标或活动窗口所在 display 定位，不固定落到 screen 0。
   - 不可恢复时 fallback 到主屏居中。
6. 最小尺寸：
   - 复查 `minWidth: 1100`、`minHeight: 720` 是否过大。
   - 若小屏 Windows 设备受影响，降低最小值并补响应式断言。

取舍：Mica/acrylic 与透明背景会增加 Windows 版本差异测试。收益是第一屏 native feel 提升最大。

验证：

```powershell
pnpm vitest run tests/renderer/window-workband.test.ts
pnpm build
pnpm smoke:electron
pnpm smoke:performance
```

完成标准：

- 窗口拖动不依赖高频 renderer -> main IPC。
- Windows 11 可见 Mica 或明确 fallback。
- Windows 10 fallback 无白屏、无透明穿帮。
- 主窗口重启恢复位置。
- caption buttons hover/pressed/close-red 行为被 screenshot 或 Playwright 检查覆盖。

## Phase 3：WebView 行为清理

目标：删掉 Windows 用户最容易感到“这是网页”的交互。

动作：

1. Cursor：
   - 移除 `src/renderer/styles/base.css` 的全局 `button { cursor: pointer; }`。
   - 默认 `cursor: default`。
   - 只给文本输入、resize separator、真实链接、可拖动区域配置对应 cursor。
   - list row、tab、toolbar button 不显示 pointer。
2. Text selection：
   - chrome 默认 `user-select: none`。
   - 明确允许 chat message、code block、diff preview、terminal、input、textarea 选择文本。
3. Scroll：
   - `chat-transcript-panel.tsx` 默认不使用 smooth scroll。
   - 用户主动点击“滚动到底部”时可短动画，但必须 obey reduced motion。
4. Native context menu：
   - 在 `webContents.on('context-menu')` 中按 editable/non-editable 分支。
   - editable：Copy/Cut/Paste/Select All。
   - 非 editable chrome：无菜单或最小菜单。
   - code/diff/message：Copy / Copy selection。
5. Native confirmation：
   - 新增 preload confirmation API。
   - 主进程用 `dialog.showMessageBox()`。
   - 替换 `GitWorkbench.tsx` 的 `window.confirm()`。
6. Dialog focus：
   - `TaskCreateDialog` 保留 DOM dialog，但补齐 Escape、focus trap、initial focus、aria-modal。
   - Settings 若继续主窗口内 modal，必须同样补齐 focus/escape；若后续独立窗口，再新增 `settings.html` entry。
7. Keyboard：
   - Escape：关闭当前 popover/dialog/drawer 或取消 transient 状态。
   - Tab：按视觉顺序。
   - Enter/Space：触发焦点按钮。
   - 历史列表、任务列表、Git 文件列表支持 type-ahead。
8. IME：
   - 手工验证中文拼音输入：chat composer、task create、settings provider fields、Git commit textarea。
   - smoke 不替代 IME 手工记录。

取舍：去掉 pointer 会让部分网页习惯用户感觉“不够可点”。Windows 桌面感优先。

验证：

```powershell
pnpm vitest run tests/renderer/animation-config.test.ts
pnpm vitest run tests/renderer/task-create-dialog.test.ts
pnpm build
pnpm smoke:electron
```

完成标准：

- 常用 row/tab/button computed cursor 是 `default`。
- Chrome 区域文本不可选。
- message/code/diff/terminal 可选。
- Git 危险操作不再调用 `window.confirm()`。
- Escape/focus 行为有测试。

## Phase 4：Windows 视觉 token 与系统主题

目标：让颜色、字体、状态跟随 Windows，而不是静态网页主题。

动作：

1. 字体：
   - chrome font stack 以 `Segoe UI Variable`, `Segoe UI`, `Microsoft YaHei UI` 优先。
   - 内容和代码区保留各自语义字体。
2. System theme：
   - 主进程读取 Electron `nativeTheme`。
   - renderer 只消费主进程传来的 resolved theme。
   - CSS 设置 `color-scheme`。
   - forced-colors/high contrast 必须有独立 smoke 或手工记录，不能只按 light/dark 判断。
3. Accent：
   - 读取 Windows accent color。
   - `--accent` 默认跟随系统。
   - Roc 品牌色只作为 fallback。
4. Surface：
   - app shell 外层改为 material-friendly。
   - panel/card 使用 surface token。
   - popover/modal shadow 只用于浮层，不用于窗口外层。
5. Motion：
   - 普通 view 切换默认 cut。
   - 快速状态变化使用短 transition。
   - 所有 motion obey reduced motion。
6. Loading：
   - <200ms 操作不显示 loading。
   - 200ms-2s 用 spinner/status line。
   - 长操作用进度或具体状态。
   - 不为快速操作做 skeleton。
7. Visual smoke：
   - 增加关键页面 screenshot：chat、tasks、settings、files/git workbench、quick entry、tray entry。
   - 截图用于检测透明穿帮、外层阴影、caption 状态。

取舍：system accent 会降低品牌色控制。收益是更符合 Windows 用户预期。

验证：

```powershell
pnpm vitest run tests/renderer/animation-config.test.ts
pnpm build
pnpm smoke:electron
```

完成标准：

- Windows light/dark 切换无一帧闪白。
- accent 改变后 UI token 可更新。
- 外层窗口不再靠 CSS 模拟 shadow/radius。
- 截图覆盖主工作流。

## Phase 5：IPC、安全和性能测量

目标：降低 Electron 边界成本，补齐 Windows packaged 证据。

动作：

1. 拆分 `register-ipc.ts`：
   - 按 app/window/tasks/lifecycle/diagnostics/memory/settings/agent/chat/workspace/files/git/terminal/shell 分域。
   - 保留 `ipcChannels` 单一事实源。
   - 每个注册函数只接收需要的 services。
2. preload contract：
   - `preload/index.ts` 与 `RocPreloadApi` 继续编译一致。
   - 增加 exposed key 测试，防止 runtime API 与类型漂移。
3. IPC top N：
   - `timedIpc()` 已记录 timing。
   - smoke 输出 top N slow calls、top N high-frequency calls。
   - 重点证明 window drag 已不再刷 `window:set-bounds`。
4. Broadcast 过滤：
   - 审计 `broadcastToWindows([mainWindow, quickEntryWindow, trayEntryWindow], ...)`。
   - terminal/chat/task 高频事件按窗口订阅范围发送。
5. Sandbox：
   - 当前 `contextIsolation: true`、`nodeIntegration: false`，但 `sandbox: false`。
   - 单独验证 `sandbox: true` 对 preload、protocol preview、node-pty、better-sqlite3 影响。
   - 若不能开启，记录原因和补偿。
6. 外部链接：
   - 当前 `setWindowOpenHandler` deny 并 `shell.openExternal()`。
   - 增加 URL scheme allowlist。
7. 跨进程 metrics：
   - `DiagnosticsService.samplePerformance()` 不再只报告 main process。
   - 增加 Electron process metrics：main、renderer、GPU、utility。
   - artifact 标记 Electron baseline 与 Roc margin。
8. Bundle/window 策略：
   - 保留 `index/quick/tray` 三入口。
   - 若 settings 独立窗口，新增 `settings.html`。
   - quick entry 是否预热由 hotkey latency 决定；预热必须记录 idle memory 增量。

取舍：IPC 拆分和 metrics 会增加工程量。收益是后续 Windows packaged 回归更容易定位。

验证：

```powershell
pnpm vitest run tests/main/workspace-dialog-ipc.test.ts
pnpm vitest run tests/main/app-services.tasks.test.ts
pnpm typecheck
pnpm build
pnpm smoke:performance
pnpm smoke:electron
```

packaged 验收：

```powershell
pnpm package:dir
$env:ROC_SMOKE_TARGET = 'packaged'
pnpm smoke:electron
pnpm smoke:performance
Remove-Item Env:\ROC_SMOKE_TARGET
```

完成标准：

- IPC top N 出现在 smoke artifact。
- performance artifact 能区分 main/renderer/GPU/utility。
- packaged exe smoke 通过。
- native module probe 通过。

## Phase 6：Windows 发布级系统集成与可访问性

目标：补齐用户会按“真正 Windows 应用”判断的发布、系统和辅助功能表面。

动作：

1. URL scheme / deeplink：
   - 保留现有 `roc-preview://workspace/pdf/...` 作为内部 preview protocol，不把它当系统 deeplink。
   - 若需要从外部打开 Roc，新增明确的 app protocol，例如 `roc://open?...`。
   - app protocol 参数必须走 single-instance `second-instance` 分发到现有窗口。
   - protocol payload 只允许白名单动作：打开主窗口、打开任务、打开 workspace path。
2. File association：
   - 只有在产品需要双击特定文件打开 Roc 时才配置。
   - 若新增，`electron-builder.yml` 必须声明 ext/name/role，main process 必须验证路径在用户可访问位置。
   - smoke 只验证 handler wiring；真实双击需要手工 packaged 记录。
3. Windows toast：
   - 后台任务失败、等待用户确认、长任务完成走系统通知。
   - 通知点击只聚焦现有窗口并导航到任务详情，不新建第二实例。
   - 尊重 `notifications.lowDistraction`，默认只发 failure/confirmation。
4. Taskbar / Jump List：
   - 若产品需要系统级快捷入口，使用 Electron Windows taskbar API 增加 user tasks 或 Jump List。
   - 默认入口只保留高频动作：打开 Roc、Quick Entry、最近任务。
   - 长任务可用 taskbar progress 表示运行中/失败状态，完成或取消后必须清除。
   - 没有落地前，UI 不暗示存在 taskbar 快捷动作。
5. Native clipboard：
   - 继续允许 `navigator.clipboard.writeText()` 处理纯文本 code/id。
   - 对 markdown/code/diff 复制增加主进程 clipboard API 时，明确写入 text/html 两种格式。
   - 不为普通按钮或 chrome 文本加入 clipboard 行为。
6. Native file dialogs / export：
   - 打开 workspace/file 继续走主进程 `dialog.showOpenDialog()`。
   - diagnostic package、导出文件、另存内容必须走 `dialog.showSaveDialog()` 或明确的系统文件保存路径。
   - 不使用浏览器下载栏、隐藏 `<a download>` 或 DOM 模拟 save panel。
7. File drag-and-drop：
   - 只对文件工作台/附件区域开放 drop。
   - drop payload 必须解析为 Windows file path，拒绝 URL/html/text 混入。
   - 拖入目录和超大文件给出可见错误。
8. Release identity：
   - `appId`、`productName`、`executableName`、icon、version 保持一致。
   - 增加 packaged artifact 对 icon、AUMID、taskbar grouping、Jump List/taskbar progress absent-or-present 的检查记录。
   - 若进入发布分发，补 code signing、installer target、update channel；本计划不把 unsigned `dir` 产物当正式发布。
9. Auto-update：
   - 若使用 `electron-updater` 或 MSIX/update channel，先定义 rollback、下载失败、校验失败和用户可见状态。
   - 没有 updater 前，设置页不得暗示自动更新能力。
10. Crash reporter：
   - 现有 `LogService` / diagnostic package 继续保留。
   - 若接入 Sentry/Bugsnag，main/renderer/preload 崩溃都必须有 release、commit、process kind。
   - crash 上报必须遵守本地隐私边界，diagnostic package 保持 redaction。
11. Accessibility：
   - Narrator 能读主导航、chat composer、task detail、Git workbench、settings。
   - Tab 顺序与视觉顺序一致。
   - 系统字体放大后无截断；固定高度控件要复查。
   - 颜色对比至少 WCAG AA。
   - Windows high contrast / forced-colors 下关键控件仍可辨认。

取舍：发布级系统集成会扩大验收面，并引入签名、通知权限、更新失败等非 UI 问题。收益是 Roc 不再只是在开发机上“能跑”，而是符合 Windows 桌面应用发布预期。

验证：

```powershell
pnpm vitest run tests/main/native-packaging.test.ts
pnpm vitest run tests/main/workspace-dialog-ipc.test.ts
pnpm typecheck
pnpm build
pnpm package:dir
$env:ROC_SMOKE_TARGET = 'packaged'
pnpm smoke:electron
Remove-Item Env:\ROC_SMOKE_TARGET
```

手工验收记录：

- Windows 11：Taskbar icon/grouping、Jump List/taskbar progress、toast 点击、AUMID、tray menu、hotkey、Narrator、IME。
- Windows 10 fallback：窗口背景、tray、通知、签名/启动无 SmartScreen 异常记录。
- 高 DPI / 多屏：窗口恢复不跑出屏幕，quick entry 打开在预期屏幕，scaleFactor 变化后 bounds 仍有效。

完成标准：

- 系统 URL/file/notification/taskbar/update/crash 能力不存在时，UI 不暗示存在。
- 一旦存在，必须被 packaged smoke、主进程测试或手工记录覆盖。
- Narrator、Tab order、large font 至少覆盖主工作流。

## 建议执行顺序

1. Phase 0：先落 Windows native-feel scorecard 和 smoke artifact。
2. Phase 1A：single instance + AppUserModelID。
3. Phase 1B：真实 Tray + close/minimize/quit。
4. Phase 1C：openAtLogin + globalHotkey。
5. Phase 2A：native drag + caption buttons。
6. Phase 2B：Mica/acrylic + bounds restore。
7. Phase 3A：cursor/user-select/smooth scroll。
8. Phase 3B：native context menu + native confirmation。
9. Phase 4：theme/accent/font/motion。
10. Phase 5：IPC 拆分、metrics、packaged smoke 强化。
11. Phase 6A：URL scheme / notifications / clipboard / drag-and-drop。
12. Phase 6B：signing / installer / updater / crash reporter / accessibility。

## Ship-readiness 覆盖矩阵

| native-feel 审计面 | 覆盖阶段 | 当前状态 |
|---|---|---|
| Cold launch / first input | Phase 0、Phase 5 | 已有 ready-to-show timing 和 startup load policy；缺 renderer interactive、first input latency |
| Window / focus / multi-monitor | Phase 1、Phase 2 | 有 frameless window 和 basic controls；缺 single-instance、bounds restore、多屏校验 |
| Input / cursor / context menu / IME | Phase 3 | 已发现全局 pointer、smooth scroll、`window.confirm()`；缺 native context menu、focus trap、IME 记录 |
| Visual / material / theme | Phase 2、Phase 4 | 有 CSS token；缺 Mica/acrylic、system accent、system font 优先、dark mode、high contrast/forced-colors 实装 |
| Scrolling / motion | Phase 3、Phase 4 | 有 `MotionConfig reducedMotion="user"`；缺禁止 smooth scroll 和 route fade 的测试 |
| Performance / memory truth | Phase 0、Phase 5 | 有 main RSS/heap sample；缺 Electron `getAppMetrics()`、renderer/GPU/utility、IPC top N |
| System integration | Phase 1、Phase 6 | 有内部 protocol、config settings；缺 real Tray、hotkey、openAtLogin、toast、taskbar/Jump List、deeplink、file association |
| Packaging / release | Phase 0、Phase 6 | 有 `package:dir`、native module probe、packaged smoke；缺 signing、installer、auto-update |
| Crash / diagnostics | Phase 5、Phase 6 | 有 logs 和 diagnostic package；缺 crash reporter |
| Accessibility | Phase 3、Phase 6 | 有局部 ARIA；缺 Narrator、large font、contrast、full keyboard smoke |

## 总体验收矩阵

每个 milestone 至少跑：

```powershell
pnpm typecheck
pnpm build
```

涉及 renderer 行为：

```powershell
pnpm vitest run tests/renderer/animation-config.test.ts
pnpm smoke:electron
```

涉及 main/window/IPC：

```powershell
pnpm vitest run tests/main/workspace-dialog-ipc.test.ts
pnpm smoke:electron
```

涉及 Windows packaged 行为：

```powershell
pnpm package:dir
$env:ROC_SMOKE_TARGET = 'packaged'
pnpm smoke:electron
pnpm smoke:performance
Remove-Item Env:\ROC_SMOKE_TARGET
```

涉及性能：

```powershell
pnpm smoke:performance
```

涉及 release/system integration：

```powershell
pnpm vitest run tests/main/native-packaging.test.ts
pnpm package:dir
$env:ROC_SMOKE_TARGET = 'packaged'
pnpm smoke:electron
Remove-Item Env:\ROC_SMOKE_TARGET
```

涉及 accessibility / IME / 多屏 / 高 DPI：

- 必须保留手工验收记录，smoke 只能覆盖 DOM 和窗口状态。
- 记录 Windows 版本、DPI、屏幕数量、输入法、Narrator 状态。
- 任何结论都要附截图、smoke artifact 或日志路径。

完成定义：

- 所有变更只服务 Windows Roc.exe。
- 行为变更有 focused test。
- renderer/window contract 变更跑 `pnpm smoke:electron`。
- packaged 行为变更跑 `pnpm package:dir` 和 packaged smoke。
- native-feel 结论必须有截图、smoke artifact、日志或手工检查记录。
- 未实现的系统能力必须在 UI 中保持诚实，不展示成已支持。
